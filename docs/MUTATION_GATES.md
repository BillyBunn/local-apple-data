# Mutation Gates

The current plugin is local-only and read-mostly. Approved write tools: `reminders apply`, `reminders_apply_change`, `reminders apply-list`, `reminders_apply_list_change`, `icloud-drive apply`, `icloud_drive_apply_change`, `calendar apply`, `calendar_apply_change`, `calendar apply-calendar`, `calendar_apply_calendar_change`, `contacts apply`, `contacts_apply_change`, `notes apply`, `notes_apply_change`, `mail apply`, `mail_apply_change`, `mail apply-mailbox`, `mail_apply_mailbox_change`, `mail apply-cleanup`, `mail_apply_cleanup`, `photos apply`, `photos_apply_change`, `messages apply`, `messages_apply_change`, `filesystem apply`, `filesystem_apply_change`, `shortcuts apply`, and `shortcuts_apply_run`.

The public MCP inventory contains 14 apply-capable tools. CLI aliases and the paired non-mutating planning tools do not increase that public MCP apply-tool count.

Those tools are limited to Reminders create, complete, uncomplete, due-date update, title update, notes update, priority update, exact URL update/clear through the plan/apply/read-back contract in `docs/V1_136_REMINDERS_URL_WRITE_DESIGN.md`, exact absolute display-alarm set/clear through the plan/apply/read-back contract in `docs/V1_137_REMINDERS_ABSOLUTE_DISPLAY_ALARM_WRITE_DESIGN.md`, exact relative display-alarm set and broadened pure display-alarm clear through the plan/apply/read-back contract in `docs/V1_138_REMINDERS_RELATIVE_DISPLAY_ALARM_WRITE_DESIGN.md`, exact mixed absolute-plus-relative display-alarm set and broadened mixed display-alarm clear through the plan/apply/read-back contract in `docs/V1_176_REMINDERS_MIXED_DISPLAY_ALARM_WRITE_DESIGN.md`, exact list move through the plan/apply/read-back contract in `docs/V1_65_REMINDERS_LIST_MOVE_WRITE_DESIGN.md`, and exact-handle delete through the plan/apply/read-back contracts in `docs/V1_11_REMINDERS_WRITE_DESIGN.md` and `docs/V1_35_REMINDERS_DELETE_WRITE_DESIGN.md`, iCloud Drive create-text through the plan/apply/read-back contract in `docs/V1_12_ICLOUD_DRIVE_WRITE_DESIGN.md`, iCloud Drive append-text through the plan/apply/read-back contract in `docs/V1_18_ICLOUD_DRIVE_APPEND_WRITE_DESIGN.md`, iCloud Drive replace-text through the plan/apply/read-back contract in `docs/V1_51_ICLOUD_DRIVE_REPLACE_WRITE_DESIGN.md`, iCloud Drive create-folder through the plan/apply/read-back contract in `docs/V1_52_ICLOUD_DRIVE_FOLDER_CREATE_WRITE_DESIGN.md`, iCloud Drive trash-text through the plan/apply/read-back absence-proof contract in `docs/V1_53_ICLOUD_DRIVE_TRASH_WRITE_DESIGN.md`, iCloud Drive rename-text/copy-text/move-text through the plan/apply/read-back contract in `docs/V1_54_ICLOUD_DRIVE_RENAME_COPY_MOVE_WRITE_DESIGN.md`, iCloud Drive exact folder rename through the plan/apply/read-back metadata-proof contract in `docs/V1_60_ICLOUD_DRIVE_FOLDER_RENAME_WRITE_DESIGN.md`, iCloud Drive exact folder Trash through the plan/apply/read-back metadata and absence-proof contracts in `docs/V1_61_ICLOUD_DRIVE_FOLDER_TRASH_WRITE_DESIGN.md` and `docs/V1_146_ICLOUD_DRIVE_NON_EMPTY_FOLDER_TRASH_WRITE_DESIGN.md`, iCloud Drive exact folder move through the plan/apply/read-back metadata-proof contract in `docs/V1_62_ICLOUD_DRIVE_FOLDER_MOVE_WRITE_DESIGN.md`, iCloud Drive exact selected-folder copy through the plan/apply/read-back metadata-proof contracts in `docs/V1_63_ICLOUD_DRIVE_FOLDER_COPY_WRITE_DESIGN.md` and `docs/V1_147_ICLOUD_DRIVE_NON_EMPTY_FOLDER_COPY_WRITE_DESIGN.md`, iCloud Drive exact selected-folder permanent delete through the plan/apply/read-back metadata, hidden-staging, and absence-proof contract in `docs/V1_67_ICLOUD_DRIVE_FOLDER_DELETE_WRITE_DESIGN.md`, iCloud Drive exact text-file permanent delete through the plan/apply/read-back content-hash, exact file identity, random-only hidden-staging, and absence-proof contract in `docs/V1_68_ICLOUD_DRIVE_DELETE_TEXT_WRITE_DESIGN.md`, iCloud Drive exact regular-file rename/copy/move through the plan/apply/read-back metadata-proof contract in `docs/V1_127_ICLOUD_DRIVE_REGULAR_FILE_RENAME_COPY_MOVE_WRITE_DESIGN.md`, iCloud Drive exact local regular-file import through the plan/apply/read-back metadata-proof contract in `docs/V1_129_ICLOUD_DRIVE_IMPORT_FILE_WRITE_DESIGN.md`, iCloud Drive exact regular-file replace through the plan/apply/read-back metadata-proof contract in `docs/V1_130_ICLOUD_DRIVE_REPLACE_FILE_WRITE_DESIGN.md`, iCloud Drive exact regular-file Trash through the plan/apply/read-back metadata-proof contract in `docs/V1_131_ICLOUD_DRIVE_TRASH_FILE_WRITE_DESIGN.md`, Calendar create-event through the plan/apply/read-back contract in `docs/V1_13_CALENDAR_WRITE_DESIGN.md`, Calendar exact-event update through the plan/apply/read-back contract in `docs/V1_34_CALENDAR_UPDATE_WRITE_DESIGN.md`, Calendar exact-event delete through the plan/apply/read-back absence-proof contract in `docs/V1_36_CALENDAR_DELETE_WRITE_DESIGN.md`, Calendar explicit all-day support through the plan/apply/read-back contract in `docs/V1_55_CALENDAR_ALL_DAY_WRITE_DESIGN.md`, Calendar exact alarm-offset support through the plan/apply/read-back contract in `docs/V1_56_CALENDAR_ALARM_WRITE_DESIGN.md`, Calendar exact target-calendar selection and move support through the plan/apply/read-back contract in `docs/V1_86_CALENDAR_TARGET_CALENDAR_MOVE_WRITE_DESIGN.md`, Calendar explicit timed-event time-zone support through the plan/apply/read-back contract in `docs/V1_87_CALENDAR_TIME_ZONE_WRITE_DESIGN.md`, Calendar exact absolute display-alarm support through the plan/apply/read-back contract in `docs/V1_88_CALENDAR_ABSOLUTE_ALARM_WRITE_DESIGN.md`, Calendar simple count-, end-date-, or explicit-unbounded recurrence create through the plan/apply/read-back contract in `docs/V1_89_CALENDAR_RECURRENCE_WRITE_DESIGN.md`, Calendar recurrence end-date bounds through the plan/apply/read-back contract in `docs/V1_109_CALENDAR_RECURRENCE_END_DATE_WRITE_DESIGN.md`, Calendar explicit unbounded recurrence through the plan/apply/read-back contract in `docs/V1_139_CALENDAR_UNBOUNDED_RECURRENCE_WRITE_DESIGN.md`, Calendar synthetic `LAD-TEST-*` calendar create/rename/delete through the plan/apply/read-back contract in `docs/V1_124_CALENDAR_CALENDAR_MANAGEMENT_WRITE_DESIGN.md`, Contacts create-contact through the plan/apply/read-back contract in `docs/V1_14_CONTACTS_WRITE_DESIGN.md`, Contacts exact scalar update through the plan/apply/read-back contract in `docs/V1_48_CONTACTS_UPDATE_WRITE_DESIGN.md`, Contacts exact email/phone/URL method-array replacement through the plan/apply/read-back contract in `docs/V1_69_CONTACTS_METHOD_UPDATE_WRITE_DESIGN.md`, Contacts exact delete through the plan/apply/read-back absence-proof contract in `docs/V1_49_CONTACTS_DELETE_WRITE_DESIGN.md`, Notes create-note through the plan/apply/read-back contract in `docs/V1_15_NOTES_WRITE_DESIGN.md`, Notes exact-folder note create through the plan/apply/read-back contract in `docs/V1_39_NOTES_FOLDER_CREATE_WRITE_DESIGN.md`, Notes exact child-folder create through the plan/apply/read-back parent-proof contract in `docs/V1_57_NOTES_FOLDER_CREATE_WRITE_DESIGN.md`, Notes exact-folder rename through the plan/apply/read-back metadata-proof contract in `docs/V1_58_NOTES_FOLDER_RENAME_WRITE_DESIGN.md`, Notes exact empty child-folder delete through the plan/apply/read-back absence-proof contract in `docs/V1_59_NOTES_FOLDER_DELETE_WRITE_DESIGN.md`, Notes append-text through the plan/apply/read-back contract in `docs/V1_19_NOTES_APPEND_WRITE_DESIGN.md`, Notes replace-text through the plan/apply/read-back contract in `docs/V1_34_NOTES_REPLACE_WRITE_DESIGN.md`, Notes exact-note move-to-folder through the plan/apply/read-back folder-proof contract in `docs/V1_45_NOTES_MOVE_WRITE_DESIGN.md`, Notes exact-note delete through the plan/apply/read-back absence-proof contract in `docs/V1_42_NOTES_DELETE_WRITE_DESIGN.md`, Mail create-draft through the plan/apply/read-back contract in `docs/V1_16_MAIL_DRAFT_WRITE_DESIGN.md`, Mail send-message through the irreversible-send plan/apply/read-back contract in `docs/V1_43_MAIL_SEND_WRITE_DESIGN.md`, Mail exact-message sender-only reply-message through the irreversible-send plan/apply/read-back contract in `docs/V1_44_MAIL_REPLY_WRITE_DESIGN.md`, Mail exact-message reply-all-message through the irreversible-send plan/apply/read-back contract in `docs/V1_73_MAIL_REPLY_ALL_WRITE_DESIGN.md`, Mail exact-message forward-message through the irreversible-send plan/apply/read-back contract in `docs/V1_50_MAIL_FORWARD_WRITE_DESIGN.md`, Mail exact-message mark-read/mark-unread through the Stage 1 plan/apply/read-back contract in `docs/V1_32_MAIL_TRIAGE_WRITE_DESIGN.md`, Mail exact-message flag/unflag through the plan/apply/read-back contract in `docs/V1_37_MAIL_FLAG_WRITE_DESIGN.md`, Mail exact-message archive through the plan/apply/read-back contract in `docs/V1_40_MAIL_ARCHIVE_WRITE_DESIGN.md`, Mail exact-message target-mailbox move through the plan/apply/read-back contract in `docs/V1_46_MAIL_MOVE_WRITE_DESIGN.md`, Mail exact-message cross-account target-mailbox move through the plan/apply/read-back contract in `docs/V1_66_MAIL_CROSS_ACCOUNT_MOVE_WRITE_DESIGN.md`, Mail exact-message move-to-Trash through the plan/apply/read-back contract in `docs/V1_41_MAIL_TRASH_WRITE_DESIGN.md`, Mail synthetic `LAD-TEST-*` mailbox create/rename, plus source-gated synthetic mailbox delete/cleanup only when public Mail.app deletion plus exact target-state binding, mailbox-scoped absence proof, and Mail-idle guards succeed through the plan/apply/read-back contract in `docs/V1_84_MAIL_SYNTHETIC_MAILBOX_CLEANUP_WRITE_DESIGN.md`, Photos import through the plan/apply/read-back contract in `docs/V1_17_PHOTOS_IMPORT_WRITE_DESIGN.md`, Photos exact asset favorite/hidden update through the plan/apply/read-back contract in `docs/V1_134_PHOTOS_UPDATE_FLAGS_WRITE_DESIGN.md`, Photos exact selected-asset delete through the plan/apply/read-back absence-proof contract in `docs/V1_149_PHOTOS_DELETE_WRITE_DESIGN.md`, Photos exact regular-album membership add/remove through the plan/apply/read-back contract in `docs/V1_151_PHOTOS_ALBUM_MEMBERSHIP_WRITE_DESIGN.md`, Photos exact regular-album create/rename/delete with full Photos Library authorization through the plan/apply/read-back contract in `docs/V1_154_PHOTOS_REGULAR_ALBUM_MANAGEMENT_WRITE_DESIGN.md`, Messages send-text through the plan/apply/read-back contract in `docs/V1_24_MESSAGES_SEND_TEXT_WRITE_DESIGN.md`, and Messages send-file through the plan/apply/read-back contract in `docs/V1_38_MESSAGES_SEND_FILE_WRITE_DESIGN.md`. All other write tools remain intentionally absent until each mutation class has a separate design, explicit user approval, synthetic tests, and independent read-back verification.

Later approved gates also include Notes exact empty child-folder move (`docs/V1_158_NOTES_FOLDER_MOVE_WRITE_DESIGN.md`), Notes rich-text body create/replace (`docs/V1_179_NOTES_RICH_TEXT_BODY_WRITE_DESIGN.md`), the bounded home-directory Filesystem reuse of the iCloud Drive gates, and exact identifier-bound Shortcuts run (`docs/V1_180C_SHORTCUTS_RUN_WRITE_DESIGN.md`). Contacts note contracts are designed and synthetic-testable, but this local helper lacks the restricted Contacts-notes entitlement, so live note operations fail closed with `contacts_note_unavailable` before mutation.

For replace-file they require a matching approval token, explicit confirmation, one exact non-text non-package regular-file handle, expected target metadata SHA-256, one caller-selected local non-text non-package regular file outside the configured iCloud Drive root, private source identity/content binding, source/target extension match, target metadata drift refusal, source preservation proof, byte replacement proof, metadata-only target read-back, `source_path_returned:false`, `source_hash_returned:false`, `content_text_returned:false`, `content_hash_returned:false`, no source path/hash return, no content hash return, and no raw path return.

iCloud Drive exact regular-file Trash through the plan/apply/read-back metadata-proof contract in `docs/V1_131_ICLOUD_DRIVE_TRASH_FILE_WRITE_DESIGN.md`.

For trash-file they require a matching approval token, explicit confirmation, one exact non-text non-package regular-file handle, expected target metadata SHA-256, target metadata drift refusal, no-follow/package/symlink traversal refusal, recoverable Trash move, original absence proof, metadata-only read-back, `trash_path_returned:false`, `content_text_returned:false`, `content_hash_returned:false`, no content hash return, and no raw path return.

Reminders exact same-source list move through the plan/apply/read-back contract in `docs/V1_65_REMINDERS_LIST_MOVE_WRITE_DESIGN.md`.

Reminders exact URL update/clear through the plan/apply/read-back contract in `docs/V1_136_REMINDERS_URL_WRITE_DESIGN.md`.

For Reminder URL update/clear they require a matching approval token, explicit confirmation, exact opaque reminder handle, expected title, exact expected URL presence, exact expected URL SHA-256 when a URL is present, allow-listed URL scheme, ASCII-only URL input, EventKit apply, hash-only URL read-back proof for update, absence proof for clear, and no raw URL return.

Reminders exact absolute display-alarm set/clear through the plan/apply/read-back contract in `docs/V1_137_REMINDERS_ABSOLUTE_DISPLAY_ALARM_WRITE_DESIGN.md`.

For Reminder absolute display-alarm set/clear they require a matching approval token, explicit confirmation, exact opaque reminder handle, expected title, expected completed state, exact expected alarm count, exact expected alarm-state SHA-256 when alarms are present, bounded timezone-explicit absolute alarm dates, EventKit apply, exact date read-back proof for set, absence proof for clear, and no raw alarm state return.

Reminders exact relative display-alarm set and broadened pure display-alarm clear through the plan/apply/read-back contract in `docs/V1_138_REMINDERS_RELATIVE_DISPLAY_ALARM_WRITE_DESIGN.md`.

For Reminder relative display-alarm set and pure display-alarm clear they require a matching approval token, explicit confirmation, exact opaque reminder handle, expected title, expected completed state, exact expected alarm count, exact expected alarm-state SHA-256 when alarms are present, bounded integer minute offsets for set, EventKit apply, exact offset read-back proof for set, absence proof for pure display-alarm clear, and no raw alarm state return.

Reminders exact mixed absolute-plus-relative display-alarm set/clear through the plan/apply/read-back contract in `docs/V1_176_REMINDERS_MIXED_DISPLAY_ALARM_WRITE_DESIGN.md`.

For Reminder mixed display-alarm set and mixed display-alarm clear they require a matching approval token, explicit confirmation, exact opaque reminder handle, expected title, expected completed state, exact expected alarm count, exact expected alarm-state SHA-256 when alarms are present, bounded integer minute offsets plus bounded timezone-explicit absolute alarm dates for set, EventKit apply, exact mixed offset/date read-back proof for set, absence proof for clear, and no raw alarm state return. The v1.176 gate approves exact mixed absolute-plus-relative display-alarm set/clear mutation with exact Reminder handle, expected title, expected completed state, expected alarm count, expected alarm-state SHA-256 when present, bounded relative offsets plus timezone-explicit absolute alarm dates, EventKit apply, exact mixed offset/date read-back or absence proof, and no raw alarm state return.

Reminders exact start-date set/clear through the plan/apply/read-back contract in `docs/V1_177_REMINDERS_START_DATE_WRITE_DESIGN.md`.

For Reminder start-date set/clear they require a matching approval token, explicit confirmation, exact opaque reminder handle for update-start-date, expected title, exact expected current start-date state for update, a date-only or timezone-explicit start date that is on or before the due date when both are present, EventKit apply, exact start-date read-back proof for set, and start-date absence proof for clear.

Reminders exact recurrence create/update/clear through the plan/apply/read-back contract in `docs/V1_177_REMINDERS_RECURRENCE_WRITE_DESIGN.md`.

For Reminder recurrence create/update/clear they reuse the exact Calendar recurrence payload contract and shared `_normalize_recurrence` builder, require a matching approval token, explicit confirmation, exact opaque reminder handle for update-recurrence, expected title, exact expected recurrence shape for update, a due date anchor for any recurring reminder, `recurrence_frequency`/`recurrence_interval`/`recurrence_count`/`recurrence_end_date`/`recurrence_unbounded`/`recurrence_weekdays`/`recurrence_month_days`/`recurrence_month_weekdays`/`recurrence_year_months`/`recurrence_year_month_days`/`recurrence_year_month_weekdays`/`recurrence_year_days`/`recurrence_year_weeks`/`recurrence_set_positions` bounded exactly as Calendar recurrence, `clear_recurrence:true` for clearing, EventKit apply, exact recurrence-shape read-back proof for create or replace, and recurrence absence proof for clear.

Calendar simple count-bound daily/weekly/monthly/yearly recurrence create and date-only all-day inference through the plan/apply/read-back contract in `docs/V1_90_CALENDAR_RECURRENCE_DATE_ONLY_WRITE_DESIGN.md`.

Calendar explicit default-calendar create planning through the plan-only exact-handle resolver in `docs/V1_91_CALENDAR_DEFAULT_CALENDAR_CREATE_WRITE_DESIGN.md`.

Calendar exact availability create/update through the plan/apply/read-back contract in `docs/V1_92_CALENDAR_AVAILABILITY_WRITE_DESIGN.md`.

Calendar simple count-bound recurrence add-to-non-recurring-event update through the plan/apply/read-back contract in `docs/V1_93_CALENDAR_RECURRENCE_UPDATE_WRITE_DESIGN.md`.

Calendar exact allow-listed event URL create/update through the plan/apply/read-back contract in `docs/V1_94_CALENDAR_EVENT_URL_WRITE_DESIGN.md`.

Calendar safe non-HTTP event URL schemes through the plan/apply/read-back contract in `docs/V1_133_CALENDAR_SAFE_NON_HTTP_EVENT_URL_WRITE_DESIGN.md`.

Calendar exact event URL clearing through the plan/apply/read-back contract in `docs/V1_95_CALENDAR_EVENT_URL_CLEAR_WRITE_DESIGN.md`.

Calendar selected recurring occurrence delete through the plan/apply/read-back contract in `docs/V1_96_CALENDAR_RECURRING_OCCURRENCE_DELETE_WRITE_DESIGN.md`.

Calendar future-event recurring span delete through the plan/apply/read-back contract in `docs/V1_97_CALENDAR_RECURRING_FUTURE_DELETE_WRITE_DESIGN.md`.

Calendar whole-series recurring-event delete through the plan/apply/read-back contract in `docs/V1_98_CALENDAR_RECURRING_SERIES_DELETE_WRITE_DESIGN.md`.

Calendar weekly weekday recurrence selection through the plan/apply/read-back contract in `docs/V1_99_CALENDAR_WEEKLY_WEEKDAY_RECURRENCE_WRITE_DESIGN.md`.

Calendar first-visible and mid-series recurrence clearing through the plan/apply/read-back contract in `docs/V1_100_CALENDAR_RECURRENCE_CLEAR_WRITE_DESIGN.md`.

Calendar mid-series recurrence replacement through the plan/apply/read-back contract in `docs/V1_126_CALENDAR_RECURRENCE_REPLACEMENT_WRITE_DESIGN.md`.

Calendar future-series recurring-event title/plain-location/notes update through the plan/apply/read-back contract in `docs/V1_167_CALENDAR_FUTURE_SERIES_SCALAR_UPDATE_WRITE_DESIGN.md`.

Calendar future-series recurring-event timed reschedule through the plan/apply/read-back contract in `docs/V1_168_CALENDAR_FUTURE_SERIES_RESCHEDULE_WRITE_DESIGN.md`.

Calendar future-series recurring-event availability update through the plan/apply/read-back contract in `docs/V1_169_CALENDAR_FUTURE_SERIES_AVAILABILITY_WRITE_DESIGN.md`.

Calendar future-series recurring-event event URL set/clear through the plan/apply/read-back contract in `docs/V1_170_CALENDAR_FUTURE_SERIES_EVENT_URL_WRITE_DESIGN.md`.

Calendar future-series recurring-event structured-location set/clear through the plan/apply/read-back contract in `docs/V1_171_CALENDAR_FUTURE_SERIES_STRUCTURED_LOCATION_WRITE_DESIGN.md`.

Calendar future-series recurring-event display-alarm set/clear through the plan/apply/read-back contract in `docs/V1_172_CALENDAR_FUTURE_SERIES_DISPLAY_ALARM_WRITE_DESIGN.md`.

Calendar future-series recurring-event action-alarm set/clear through the plan/apply/read-back contract in `docs/V1_173_CALENDAR_FUTURE_SERIES_ACTION_ALARM_WRITE_DESIGN.md`.

Calendar future-series recurring-event all-day set/clear/date-only reschedule through the plan/apply/read-back contract in `docs/V1_174_CALENDAR_FUTURE_SERIES_ALL_DAY_WRITE_DESIGN.md`.

Calendar future-series recurring-event target-calendar move through the plan/apply/read-back contract in `docs/V1_175_CALENDAR_FUTURE_SERIES_CALENDAR_MOVE_WRITE_DESIGN.md`.

Calendar monthly day-of-month recurrence selection through the plan/apply/read-back contract in `docs/V1_101_CALENDAR_MONTHDAY_RECURRENCE_WRITE_DESIGN.md`.

Calendar structured event location create/update through the plan/apply/read-back contract in `docs/V1_102_CALENDAR_STRUCTURED_LOCATION_WRITE_DESIGN.md`.

Calendar structured event location clearing through the plan/apply/read-back contract in `docs/V1_107_CALENDAR_STRUCTURED_LOCATION_CLEAR_WRITE_DESIGN.md`.

Calendar recurrence end-date bounds through the plan/apply/read-back contract in `docs/V1_109_CALENDAR_RECURRENCE_END_DATE_WRITE_DESIGN.md`.

Calendar monthly weekday recurrence selection through the plan/apply/read-back contract in `docs/V1_113_CALENDAR_MONTHLY_WEEKDAY_RECURRENCE_WRITE_DESIGN.md`.

Calendar selected recurring occurrence scalar update through the plan/apply/read-back contract in `docs/V1_114_CALENDAR_SELECTED_OCCURRENCE_UPDATE_WRITE_DESIGN.md`.
Calendar selected recurring occurrence timed reschedule through the plan/apply/read-back contract in `docs/V1_115_CALENDAR_SELECTED_OCCURRENCE_RESCHEDULE_WRITE_DESIGN.md`.
Calendar selected recurring occurrence availability update through the plan/apply/read-back contract in `docs/V1_116_CALENDAR_SELECTED_OCCURRENCE_AVAILABILITY_WRITE_DESIGN.md`.
Calendar selected recurring occurrence event URL set/clear through the plan/apply/read-back contract in `docs/V1_117_CALENDAR_SELECTED_OCCURRENCE_EVENT_URL_WRITE_DESIGN.md`.
Calendar selected recurring occurrence structured location set/clear through the plan/apply/read-back contract in `docs/V1_118_CALENDAR_SELECTED_OCCURRENCE_STRUCTURED_LOCATION_WRITE_DESIGN.md`.
Calendar selected recurring occurrence display alarm set/clear through the plan/apply/read-back contract in `docs/V1_119_CALENDAR_SELECTED_OCCURRENCE_DISPLAY_ALARM_WRITE_DESIGN.md`.
Calendar selected recurring occurrence action alarm set/clear through the plan/apply/read-back contract in `docs/V1_120_CALENDAR_SELECTED_OCCURRENCE_ACTION_ALARM_WRITE_DESIGN.md`.
Calendar selected recurring occurrence all-day set/clear/date-only reschedule through the plan/apply/read-back contract in `docs/V1_121_CALENDAR_SELECTED_OCCURRENCE_ALL_DAY_WRITE_DESIGN.md`.

Calendar synthetic `LAD-TEST-*` calendar create/rename/delete through the plan/apply/read-back contract in `docs/V1_124_CALENDAR_CALENDAR_MANAGEMENT_WRITE_DESIGN.md`.

For Calendar synthetic calendar management they require a matching approval token, explicit confirmation, one exact source or target `calendar:calendar:v1:` handle, synthetic `LAD-TEST-*` title, no raw source/account identifier input, source safe-hash binding and read-only/subscribed/immutable source refusal for create, target safe-hash binding for rename/delete, duplicate-title refusal in the same source for rename, non-default/non-subscribed/non-immutable/writable target proof for rename/delete, empty-calendar proof in the 1900-01-01 through 2100-01-01 safety window before rename/delete, event-only target proof for delete, EventKit `saveCalendar` apply for create/rename, EventKit `removeCalendar` apply for delete, source/title read-back for create, title read-back for rename, and absence proof for delete.

For recurrence create they require a matching approval token, explicit confirmation, `recurrence_frequency` of daily, weekly, monthly, or yearly, optional `recurrence_weekdays` only for weekly recurrence, monthly recurrence, or yearly week-of-year recurrence, optional `recurrence_month_days` only for monthly recurrence, optional `recurrence_month_weekdays` only for monthly recurrence, monthly selectors never mixed with each other, optional `recurrence_year_month_days` only with `recurrence_year_months`, optional `recurrence_year_month_weekdays` only with `recurrence_year_months`, optional exactly one of `recurrence_year_months`, `recurrence_year_days`, or `recurrence_year_weeks` only when recurrence is yearly, `recurrence_year_weeks` only with exact `recurrence_weekdays`, `recurrence_interval` from 1 through 4, exactly one recurrence bound using either `recurrence_count` from 2 through 52, timezone `recurrence_end_date` within 3650 days of `start_date`, or explicit `recurrence_unbounded:true`, EventKit apply, and read-back recurrence metadata.

For recurrence update they require an exact event handle, expected current state, a currently non-recurring EventKit event, a matching approval token, explicit confirmation, `recurrence_frequency` of daily, weekly, monthly, or yearly, optional `recurrence_weekdays` only for weekly recurrence, monthly recurrence, or yearly week-of-year recurrence, optional `recurrence_month_days` only for monthly recurrence, optional `recurrence_month_weekdays` only for monthly recurrence, monthly selectors never mixed with each other, optional `recurrence_year_month_days` only with `recurrence_year_months`, optional `recurrence_year_month_weekdays` only with `recurrence_year_months`, optional exactly one of `recurrence_year_months`, `recurrence_year_days`, or `recurrence_year_weeks` only when recurrence is yearly, `recurrence_year_weeks` only with exact `recurrence_weekdays`, `recurrence_interval` from 1 through 4, exactly one recurrence bound using either `recurrence_count` from 2 through 52, timezone `recurrence_end_date` within 3650 days of `start_date`, or explicit `recurrence_unbounded:true`, EventKit apply, and read-back recurrence metadata.

For monthly weekday recurrence selection they require a matching approval token, explicit confirmation, `recurrence_frequency` of monthly, one or more exact `recurrence_weekdays`, no `recurrence_month_days` or `recurrence_month_weekdays`, `recurrence_interval` from 1 through 4, exactly one recurrence bound using either `recurrence_count` from 2 through 52, timezone `recurrence_end_date` within 3650 days of `start_date`, or explicit `recurrence_unbounded:true`, EventKit `EKRecurrenceDayOfWeek` values without week numbers through `daysOfTheWeek`, and read-back recurrence metadata.

For explicit default-calendar create planning, `use_default_calendar` is accepted only on plan, resolves the current default calendar to an exact `calendar:calendar:v1:` handle, and apply must use that approved `calendar_handle` rather than re-reading the default calendar.

For availability create/update they require a matching approval token, explicit confirmation, `availability` of busy, free, tentative, or unavailable, update-only `expected_availability` drift binding, EventKit calendar support-mask validation, EventKit apply, and read-back availability metadata.

For event URL create/update they require a matching approval token, explicit confirmation, an allow-listed `event_url`, update/delete `expected_event_url_present` and `expected_event_url_sha256` binding when a current URL exists, EventKit apply, and hash-only URL read-back proof.

For event URL clearing they require update-only `clear_event_url:true`, no `event_url`, exact `expected_event_url_present:true`, exact `expected_event_url_sha256`, a matching approval token, explicit confirmation, EventKit apply, and read-back absence proof with `event_url_cleared_verified:true`.

For selected recurring occurrence delete they require delete-only `recurrence_delete_scope:this_event`, expected recurrence presence binding, occurrence start/end identity binding, adjacent occurrence identity binding, a matching approval token, explicit confirmation, EventKit `.thisEvent` removal, selected-occurrence absence proof, and adjacent-occurrence preservation proof.

For future-event recurring span delete they require delete-only `recurrence_delete_scope:future_events`, expected recurrence presence binding, selected occurrence start/end identity binding, previous occurrence identity binding, future occurrence identity binding, a matching approval token, explicit confirmation, EventKit `.futureEvents` removal, selected/future occurrence absence proof, and previous-occurrence preservation proof.

For whole-series recurring-event delete they require delete-only `recurrence_delete_scope:all_events`, expected recurrence presence binding, selected occurrence start/end identity binding, future occurrence identity binding, bounded previous-occurrence absence proof, a matching approval token, explicit confirmation, EventKit `.futureEvents` removal from the selected first visible occurrence, selected/future occurrence absence proof, and bounded previous-occurrence absence proof.

For recurrence clearing they require update-only `clear_recurrence:true`, no scalar, calendar move, URL, alarm, availability, or recurrence-add co-mutation, exact recurrence shape binding, selected occurrence start/end identity binding, future occurrence identity binding, bounded previous-occurrence absence proof for first-visible clearing or exact previous-occurrence identity/preservation proof for mid-series `recurrence_update_scope:future_events`, a matching approval token, explicit confirmation, EventKit `.futureEvents` recurrence clear from the selected occurrence, selected-occurrence non-recurring read-back, future occurrence absence proof, and previous-occurrence absence or preservation proof.

For mid-series recurrence replacement they require update-only `recurrence_update_scope:future_events`, no scalar, calendar move, URL, alarm, availability, structured-location, or clear-recurrence co-mutation, exact expected recurrence shape binding, exact approved replacement recurrence binding, selected occurrence start/end identity binding, previous occurrence identity binding, future occurrence identity binding, a matching approval token, explicit confirmation, EventKit `.futureEvents` save with a new `EKRecurrenceRule`, selected-occurrence replacement recurrence read-back, future occurrence replacement recurrence proof, and previous-occurrence preservation proof.

For future-series recurring-event scalar update they require update-only `recurrence_update_scope:future_events` without recurrence fields, exact expected recurrence shape binding, selected occurrence start/end identity binding, previous occurrence identity binding, future occurrence identity binding, title/plain-location/notes-only mutation, no time/all-day/calendar/availability/event-URL/structured-location/alarm/recurrence co-mutation, matching approval token, explicit confirmation, EventKit `.futureEvents` save, selected and future occurrence recurrence-shape plus scalar read-back proof, and previous-occurrence preservation proof.

For future-series recurring-event timed reschedule they require update-only `recurrence_update_scope:future_events` without recurrence fields, exact expected recurrence shape binding, selected occurrence start/end identity binding, previous occurrence identity binding, future occurrence identity binding, timed start/end/time-zone mutation, explicit `time_zone` and `expected_time_zone`, no all-day/calendar/availability/event-URL/structured-location/alarm/recurrence co-mutation, matching approval token, explicit confirmation, EventKit `.futureEvents` save, selected and future occurrence recurrence-shape plus timed read-back proof, original selected/future slot absence-or-approved-replacement proof when dates move, and previous-occurrence preservation proof.

For future-series recurring-event availability update they require update-only `recurrence_update_scope:future_events` without recurrence fields, exact expected recurrence shape binding, selected occurrence start/end identity binding, previous occurrence identity binding, future occurrence identity binding, proposed `availability`, required `expected_availability`, target-calendar support-mask validation, no scalar/timed/all-day/calendar/event-URL/structured-location/alarm/recurrence co-mutation, matching approval token, explicit confirmation, EventKit `.futureEvents` save, selected and future occurrence recurrence-shape plus availability read-back proof, and previous-occurrence preservation proof.

For future-series recurring-event event URL set/clear they require update-only `recurrence_update_scope:future_events` without recurrence fields, exact expected recurrence shape binding, selected occurrence start/end identity binding, previous occurrence identity binding, future occurrence identity binding, exact allow-listed `event_url` or update-only `clear_event_url:true`, required URL expected-state binding for clear, no scalar/timed/all-day/calendar/availability/structured-location/alarm/recurrence co-mutation, matching approval token, explicit confirmation, EventKit `.futureEvents` save, selected and future occurrence recurrence-shape plus hash-only URL read-back or absence proof, no raw URL return, and previous-occurrence preservation proof.

For future-series recurring-event structured-location set/clear they require update-only `recurrence_update_scope:future_events` without recurrence fields, exact expected recurrence shape binding, selected occurrence start/end identity binding, previous occurrence identity binding, future occurrence identity binding, bounded `structured_location` or update-only `clear_structured_location:true`, exact expected structured-location binding for clear, no scalar/timed/all-day/calendar/availability/event-URL/alarm/recurrence co-mutation, matching approval token, explicit confirmation, EventKit `.futureEvents` save, selected and future occurrence recurrence-shape plus structured-location read-back or absence proof, and previous-occurrence preservation proof.

For future-series recurring-event display-alarm set/clear they require update-only `recurrence_update_scope:future_events` without recurrence fields, exact expected recurrence shape binding, selected occurrence start/end identity binding, previous occurrence identity binding, future occurrence identity binding, bounded relative `alarm_offsets_minutes` or absolute `alarm_absolute_dates` or update-only display-alarm clear, exact expected display-alarm state binding, no scalar/timed/all-day/calendar/availability/event-URL/structured-location/action-alarm/recurrence co-mutation, matching approval token, explicit confirmation, EventKit `.futureEvents` save, selected and future occurrence recurrence-shape plus display-alarm read-back or absence proof, and previous-occurrence preservation proof.

For future-series recurring-event action-alarm set/clear they require update-only `recurrence_update_scope:future_events` without recurrence fields, exact expected recurrence shape binding, selected occurrence start/end identity binding, previous occurrence identity binding, future occurrence identity binding, exact audio `alarm_sound_name`, raw-input-only email `alarm_email_address`, or structured geofence `alarm_proximity` plus `alarm_structured_location` with explicit proposed trigger state or update-only action-alarm clear, exact expected display/audio/email/geofence alarm state binding, hash-only `alarm_email_address_sha256` output with no raw email return, no scalar/timed/all-day/calendar/availability/event-URL/structured-location/recurrence co-mutation, matching approval token, explicit confirmation, EventKit `.futureEvents` save, selected and future occurrence recurrence-shape plus action-alarm read-back or absence proof, and previous-occurrence preservation proof.

For future-series recurring-event all-day set/clear/date-only reschedule they require update-only `recurrence_update_scope:future_events` without recurrence fields, exact expected recurrence shape binding, selected occurrence start/end identity binding, previous occurrence identity binding, future occurrence identity binding, exact expected all-day/time-zone state binding, date-only proposed `start_date`/`end_date` for all-day set or same-state all-day date-only reschedule, explicit proposed `time_zone` for all-day-to-timed clear, no scalar/calendar/availability/event-URL/structured-location/alarm/recurrence co-mutation, matching approval token, explicit confirmation, EventKit `.futureEvents` save, selected and future occurrence recurrence-shape plus all-day read-back proof, original selected/future slot absence-or-approved-replacement proof, and previous-occurrence preservation proof.

For future-series recurring-event target-calendar move they require update-only `recurrence_update_scope:future_events` without recurrence fields, exact expected recurrence shape binding, selected occurrence start/end identity binding, previous occurrence identity binding, future occurrence identity binding, exact `calendar:calendar:v1:` target handle with resolved writable target-calendar binding, no scalar/timed/all-day/availability/event-URL/structured-location/alarm/recurrence co-mutation, matching approval token, explicit confirmation, EventKit `.futureEvents` save, selected and future occurrence recurrence-shape plus target-calendar read-back proof, and previous-occurrence original-calendar preservation proof.

For selected recurring occurrence scalar update they require update-only `recurrence_update_scope:this_event`, exact recurrence shape binding, selected occurrence start/end identity binding, adjacent occurrence identity binding, title/plain-location/notes-only mutation, a matching approval token, explicit confirmation, EventKit `.thisEvent` save, selected-occurrence read-back proof, and adjacent-occurrence preservation proof.
For selected recurring occurrence timed reschedule they require update-only `recurrence_update_scope:this_event`, exact recurrence shape binding, selected occurrence start/end identity binding, adjacent occurrence identity binding, timed start/end/time-zone mutation, a matching approval token, explicit confirmation, EventKit `.thisEvent` save, selected-occurrence read-back proof at the approved new time, original occurrence absence proof, and adjacent-occurrence preservation proof.
For selected recurring occurrence availability update they require update-only `recurrence_update_scope:this_event`, exact recurrence shape binding, selected occurrence start/end identity binding, adjacent occurrence identity binding, proposed `availability`, required `expected_availability`, target-calendar support-mask validation, a matching approval token, explicit confirmation, EventKit `.thisEvent` save, selected-occurrence availability read-back proof, and adjacent-occurrence preservation proof.
For selected recurring occurrence structured location set/clear they require update-only `recurrence_update_scope:this_event`, exact recurrence shape binding, selected occurrence start/end identity binding, adjacent occurrence identity plus hash-only sibling URL/plain-location/structured-location state binding, bounded `structured_location` with explicit expected structured-location absence or exact `expected_structured_location` replacement binding, update-only `clear_structured_location:true` with exact `expected_structured_location`, a matching approval token, explicit confirmation, EventKit `.thisEvent` save, selected-occurrence structured-location read-back or structured/plain-location absence proof, and adjacent-occurrence presence/recurrence/URL/plain-location/structured-location preservation proof.
For selected recurring occurrence display alarm set/clear they require update-only `recurrence_update_scope:this_event`, exact recurrence shape binding, selected occurrence start/end identity binding, adjacent occurrence identity plus hash-only sibling URL/plain-location/structured-location/alarm-state binding, exact `expected_alarm_offsets_minutes` or `expected_alarm_absolute_dates` binding, relative or absolute display-alarm-only proposed state, no audio/email/geofence alarm action state, a matching approval token, explicit confirmation, EventKit `.thisEvent` save, selected-occurrence display-alarm read-back proof, and adjacent-occurrence presence/recurrence/URL/plain-location/structured-location/alarm-state preservation proof.
For selected recurring occurrence action alarm set/clear they require update-only `recurrence_update_scope:this_event`, exact recurrence shape binding, selected occurrence start/end identity binding, adjacent occurrence identity plus hash-only sibling URL/plain-location/structured-location/alarm-state binding, exact expected display/audio/email/geofence alarm state binding, explicit proposed trigger/action state for set or clear, raw email input accepted only as plan/apply input and no raw email output, a matching approval token, explicit confirmation, EventKit `.thisEvent` save, selected-occurrence action-alarm read-back proof, and adjacent-occurrence presence/recurrence/URL/plain-location/structured-location/alarm-state preservation proof.
For selected recurring occurrence all-day set/clear/date-only reschedule they require update-only `recurrence_update_scope:this_event`, exact recurrence shape binding, selected occurrence start/end identity binding, adjacent occurrence identity plus hash-only sibling URL/plain-location/structured-location/alarm-state binding, exact expected all-day/time-zone state binding, date-only proposed start/end for all-day set or same-state all-day date-only reschedule, explicit proposed time zone for all-day-to-timed clear, a matching approval token, explicit confirmation, EventKit `.thisEvent` save, selected-occurrence all-day read-back proof, and adjacent-occurrence presence/recurrence/URL/plain-location/structured-location/alarm-state preservation proof.

For structured event location create/update they require a matching approval token, explicit confirmation, a bounded `structured_location` object with title plus optional paired latitude/longitude and radius, conflict refusal against plain `location`, optional update/delete `expected_structured_location` binding, EventKit `EKStructuredLocation` apply, and read-back structured-location proof.

For structured event location clearing they require update-only `clear_structured_location:true`, no `structured_location`, empty proposed `location`, exact `expected_structured_location`, a matching approval token, explicit confirmation, EventKit apply, and read-back absence proof with `structured_location_present:false`, `location_present:false`, and `structured_location_cleared_verified:true`.

Calendar monthly nth-weekday recurrence selection through the plan/apply/read-back contract in `docs/V1_105_CALENDAR_MONTHLY_NTH_WEEKDAY_RECURRENCE_WRITE_DESIGN.md`.

For monthly nth-weekday recurrence selection they require a matching approval token, explicit confirmation, `recurrence_frequency` of monthly, one or more `recurrence_month_weekdays` values with weekday plus week number, no `recurrence_month_days`, `recurrence_interval` from 1 through 4, exactly one recurrence bound using either `recurrence_count` from 2 through 52, timezone `recurrence_end_date` within 3650 days of `start_date`, or explicit `recurrence_unbounded:true`, EventKit `EKRecurrenceDayOfWeek` apply through `daysOfTheWeek`, and read-back recurrence metadata.

Calendar yearly month recurrence selection through the plan/apply/read-back contract in `docs/V1_106_CALENDAR_YEARLY_MONTH_RECURRENCE_WRITE_DESIGN.md`.

For yearly month recurrence selection they require a matching approval token, explicit confirmation, `recurrence_frequency` of yearly, one or more `recurrence_year_months` integers from 1 through 12, `recurrence_interval` from 1 through 4, exactly one recurrence bound using either `recurrence_count` from 2 through 52, timezone `recurrence_end_date` within 3650 days of `start_date`, or explicit `recurrence_unbounded:true`, EventKit `monthsOfTheYear` apply, and read-back recurrence metadata.

Calendar yearly month day-of-month recurrence selection through the plan/apply/read-back contract in `docs/V1_112_CALENDAR_YEARLY_MONTH_DAY_RECURRENCE_WRITE_DESIGN.md`.

For yearly month day-of-month recurrence selection they require a matching approval token, explicit confirmation, `recurrence_frequency` of yearly, one or more `recurrence_year_months` integers from 1 through 12, one or more `recurrence_year_month_days` integers from -31 through -1 or 1 through 31, no `recurrence_year_month_weekdays`, `recurrence_year_days`, or `recurrence_year_weeks`, `recurrence_interval` from 1 through 4, exactly one recurrence bound using either `recurrence_count` from 2 through 52, timezone `recurrence_end_date` within 3650 days of `start_date`, or explicit `recurrence_unbounded:true`, EventKit `monthsOfTheYear` plus `daysOfTheMonth` apply, and read-back recurrence metadata.

Calendar yearly month nth-weekday recurrence selection through the plan/apply/read-back contract in `docs/V1_111_CALENDAR_YEARLY_MONTH_NTH_WEEKDAY_RECURRENCE_WRITE_DESIGN.md`.

For yearly month nth-weekday recurrence selection they require a matching approval token, explicit confirmation, `recurrence_frequency` of yearly, one or more `recurrence_year_months` integers from 1 through 12, one or more `recurrence_year_month_weekdays` weekday plus week-number selectors from -5 through -1 or 1 through 5, no `recurrence_year_days` or `recurrence_year_weeks`, `recurrence_interval` from 1 through 4, exactly one recurrence bound using either `recurrence_count` from 2 through 52, timezone `recurrence_end_date` within 3650 days of `start_date`, or explicit `recurrence_unbounded:true`, EventKit `monthsOfTheYear` plus `daysOfTheWeek` apply, and read-back recurrence metadata.

Calendar yearly day-of-year and week-of-year recurrence selection through the plan/apply/read-back contract in `docs/V1_110_CALENDAR_YEARLY_DAY_WEEK_RECURRENCE_WRITE_DESIGN.md`.

For yearly day/week recurrence selection they require a matching approval token, explicit confirmation, `recurrence_frequency` of yearly, exactly one of `recurrence_year_days` integers from -366 through -1 or 1 through 366 or `recurrence_year_weeks` integers from -53 through -1 or 1 through 53, exact `recurrence_weekdays` when `recurrence_year_weeks` is used, `recurrence_interval` from 1 through 4, exactly one recurrence bound using either `recurrence_count` from 2 through 52, timezone `recurrence_end_date` within 3650 days of `start_date`, or explicit `recurrence_unbounded:true`, EventKit `daysOfTheYear` or `weeksOfTheYear` plus `daysOfTheWeek` apply, and read-back recurrence metadata.

Calendar custom recurrence shapes beyond approved selector-backed EventKit rules, implicit unbounded recurrence, attendees, invitations, silent default-calendar guessing or default-calendar mutation, timed-event time-zone inference, travel time, availability outside explicit support-mask validation, non-allow-listed event URL schemes, procedure alarms, and bulk Calendar mutation remain blocked.

iCloud Drive exact text-file permanent delete through the plan/apply/read-back content-hash, exact file identity, random-only hidden-staging, and absence-proof contract in `docs/V1_68_ICLOUD_DRIVE_DELETE_TEXT_WRITE_DESIGN.md`.

iCloud Drive exact regular-file rename/copy/move through the plan/apply/read-back metadata-proof contract in `docs/V1_127_ICLOUD_DRIVE_REGULAR_FILE_RENAME_COPY_MOVE_WRITE_DESIGN.md`.

For rename-file/copy-file/move-file they require a matching approval token, explicit confirmation, one exact non-text non-package regular-file handle, expected file metadata SHA-256, metadata drift refusal, no-overwrite target proof, source/target presence proof, metadata-only read-back, `content_text_returned:false`, `content_hash_returned:false`, no content hash return, and no raw path return.

iCloud Drive exact local regular-file import through the plan/apply/read-back metadata-proof contract in `docs/V1_129_ICLOUD_DRIVE_IMPORT_FILE_WRITE_DESIGN.md`.

For import-file they require a matching approval token, explicit confirmation, one caller-selected local non-text non-package regular file outside the configured iCloud Drive root, one exact target parent handle, private source identity/content binding, no-overwrite target proof, source preservation proof, metadata-only target read-back, `source_path_returned:false`, `source_hash_returned:false`, `content_text_returned:false`, `content_hash_returned:false`, no source path/hash return, no content hash return, and no raw path return.

iCloud Drive exact regular-file permanent delete through the plan/apply/read-back metadata-proof contract in `docs/V1_132_ICLOUD_DRIVE_DELETE_FILE_WRITE_DESIGN.md`.

For delete-file they require a matching approval token, explicit confirmation, one exact non-text non-package regular-file handle, expected target metadata SHA-256, target metadata drift refusal, no-follow/package/symlink traversal refusal, hidden staging identity proof, permanent unlink, original absence proof, metadata-only read-back, `staging_path_returned:false`, `trash_path_returned:false`, `content_text_returned:false`, `content_hash_returned:false`, no content hash return, and no raw path return.

Home-directory Filesystem CRUD through the plan/apply/read-back contract in `docs/V1_178_FILESYSTEM_HOME_SCOPE_WRITE_DESIGN.md`.

For home-directory Filesystem operations they reuse the exact iCloud Drive plan/apply/read-back gates re-rooted at the operator home directory (`~`), require a matching approval token, explicit confirmation, and an exact `fs:file:v1:` handle or parent handle, keep every resolved target within the home root after realpath/symlink resolution, refuse `..` or symlink escapes outside the home root, refuse other users' home directories and system paths, refuse content-read and mutation of a credential/secret denylist (`~/.ssh`, `~/.aws`, `~/.gnupg`, `~/.config/gh`, `~/.config/gcloud`, `~/.netrc`, `~/.docker/config.json`, `~/.kube`, `~/Library/Keychains`, `~/Library/Application Support/com.apple.TCC`, and any `.env` or `.env.*`) with `credential_path_blocked` while still allowing metadata-only listing, remain operator-overridable via `LOCAL_APPLE_DATA_FS_ALLOW_CREDENTIAL_PATHS=1`, use `~/.Trash` for reversible trash, and keep permanent delete behind the same hidden-staging identity-proof and absence-proof gate as iCloud delete.

Shortcuts run of one exact identifier-bound shortcut through the plan/apply/approval-token/confirm contract in `docs/V1_180C_SHORTCUTS_RUN_WRITE_DESIGN.md`.

For a Shortcuts run they require a matching approval token, explicit confirmation, and an exact `shortcuts:item:v1:` shortcut handle resolved to a stable identifier that is bound into the approval fingerprint, invoke the shortcut by argv (never a shell string) with bounded NUL-rejected input passed by temp-file path under a hard execution timeout, and prove invocation of the named shortcut only because a shortcut's arbitrary side effects are not verifiable by read-back. This is the plugin's only arbitrary-code-execution surface and is treated like the most dangerous send/delete gates.

For sequencing and later candidates, see `docs/WRITE_TOOL_ROADMAP.md`. Full CRUD expansion for Mail, Messages, Calendar, Reminders, and Notes is now tracked in `docs/V1_33_FULL_CRUD_PRIORITY_PLAN.md`; that plan is a priority gate only and does not approve new mutation exposure by itself.

## Global Requirements

Every mutating tool must satisfy all of these before exposure through CLI or MCP:

- Separate design doc for the specific surface and operation.
- Explicit user approval for the exact mutation class.
- Dry-run or preview mode that returns the planned change without applying it.
- Independent read-back after mutation.
- Idempotency story for retries and partial failures.
- Narrow input schema with no broad batch mutation by default.
- Stable warning/error codes with no raw paths, identifiers, stack traces, or personal content in logs.
- MCP annotations marking the tool as non-read-only and destructive when applicable.
- Tests using synthetic fixtures or mocked Apple framework helpers only.
- Redaction scan and runtime smoke passing before install.

The current `reminders plan` / `icloud-drive plan` / `calendar plan` / `calendar plan-calendar` / `contacts plan` / `notes plan` / `mail plan` / `mail plan-mailbox` / `mail plan-cleanup` / `photos plan` / `messages plan` / `filesystem plan` / `shortcuts plan` CLI commands and `reminders_plan_change` / `icloud_drive_plan_change` / `calendar_plan_change` / `calendar_plan_calendar_change` / `contacts_plan_change` / `notes_plan_change` / `mail_plan_change` / `mail_plan_mailbox_change` / `mail_plan_cleanup` / `photos_plan_change` / `messages_plan_change` / `filesystem_plan_change` / `shortcuts_plan_run` MCP tools are not mutating tools. They return `mutation_applied:false`, `apply_available:true`, and approval metadata only. Contacts note planning is an exception to live availability: the contract is synthetic-testable, but the live helper returns `contacts_note_unavailable` before producing an apply-capable preview.

Calendar exact audio alarms through the plan/apply/read-back contract in `docs/V1_103_CALENDAR_AUDIO_ALARM_WRITE_DESIGN.md` require a matching approval token, explicit confirmation, relative or absolute alarm triggers, a bounded bare `alarm_sound_name`, optional update/delete `expected_alarm_sound_name`, EventKit `EKAlarm.soundName` apply, and read-back sound-name proof.

Calendar exact email alarms through the plan/apply/read-back contract in `docs/V1_108_CALENDAR_EMAIL_ALARM_WRITE_DESIGN.md` require a matching approval token, explicit confirmation, relative or absolute alarm triggers, a bounded `alarm_email_address`, optional update/delete `expected_alarm_email_address_sha256`, EventKit `EKAlarm.emailAddress` apply, hash-only preview and read-back proof, and no raw email address output.

Calendar exact structured geofence alarms through the plan/apply/read-back contract in `docs/V1_104_CALENDAR_GEOFENCE_ALARM_WRITE_DESIGN.md` require exactly one `alarm_proximity` of enter or leave, one bounded `alarm_structured_location` object with title plus optional paired latitude/longitude and radius, no relative/absolute/audio alarm co-mutation, optional update/delete `expected_alarm_proximity` and `expected_alarm_structured_location` binding, EventKit `EKAlarm.proximity` plus `EKAlarm.structuredLocation` apply, and read-back geofence proof.

The current `reminders apply` CLI command and `reminders_apply_change` MCP tool are mutating tools. They require a matching approval token from the plan fingerprint, explicit confirmation, operation-specific expected state, EventKit apply, and read-back verification or delete absence proof. For Reminder URL update/clear they require a matching approval token, explicit confirmation, exact opaque reminder handle, expected title, exact expected URL presence, exact expected URL SHA-256 when a URL is present, allow-listed URL scheme, ASCII-only URL input, EventKit apply, hash-only URL read-back proof for update, absence proof for clear, and no raw URL return. For Reminder absolute/relative/mixed display-alarm set/clear they require a matching approval token, explicit confirmation, exact opaque reminder handle, expected title, expected completed state, exact expected alarm count, exact expected alarm-state SHA-256 when alarms are present, bounded timezone-explicit absolute alarm dates, bounded integer minute offsets, or both for mixed set, EventKit apply, exact date/offset read-back proof for set, absence proof for clear, and no raw alarm state return. For list-move they require a matching approval token, explicit confirmation, exact opaque reminder handle, exact opaque expected current-list handle, exact opaque same-source target-list handle, expected title, expected completion state, expected current list name checked before any already-target shortcut, EventKit apply, and `target_list_verified:true` identity proof. Delete additionally requires expected completed state, expected priority, and current notes SHA-256. MCP annotations mark `reminders_apply_change` as non-read-only, destructive, non-idempotent, and closed-world because the same static tool can delete.

Reminders exact list create/rename/empty-delete/same-source migrate-delete through the plan/apply/read-back contracts in `docs/V1_155_REMINDERS_LIST_CRUD_WRITE_DESIGN.md` and `docs/V1_156_REMINDERS_LIST_MIGRATION_DELETE_WRITE_DESIGN.md`.

For delete_list_with_migration they require matching approval token, explicit confirmation, exact source and target `reminders:list:eventkit:v1:` handles, same EventKit source, writable non-subscribed non-immutable reminder-only state, source/target safe-hash binding, source/target count binding, migration cap of 50 reminders, EventKit apply, source-empty proof, target-count proof, deleted-list absence proof, and no raw EventKit identifiers.

The current `icloud-drive apply` CLI command and `icloud_drive_apply_change` MCP tool are mutating tools. For create-folder they require a matching approval token from the plan fingerprint, explicit confirmation, exact opaque parent folder handle, no-follow parent validation, exclusive mkdir, metadata-only read-back, and existing-directory idempotency. For rename-folder they require a matching approval token, explicit confirmation, exact opaque directory handle, expected directory metadata SHA-256, metadata drift refusal, fd-relative no-follow no-overwrite rename, source/target presence proof, non-empty child preservation, `non_empty_allowed:true`, and metadata-only read-back with no content or child listing. For trash-folder they require a matching approval token, explicit confirmation, exact opaque directory handle, expected directory metadata SHA-256, metadata drift refusal, recoverable Trash move, original absence proof, `trash_path_returned:false`, `content_text_returned:false`, `content_hash_returned:false`, `non_empty_allowed:true`, and metadata-only read-back. For delete-folder they require a matching approval token, explicit confirmation, one exact directory handle, expected directory metadata SHA-256, private bounded source-tree binding, metadata/tree drift refusal, hidden/symlink/package/tree-size refusal, hidden staging identity proof, bounded permanent staged-tree removal, original absence proof, `verified_absent:true`, `permanently_deleted:true` only on successful removal, `trash_path_returned:false`, `staging_path_returned:false`, `content_text_returned:false`, `content_hash_returned:false`, `non_empty_allowed:true`, no child listing, and metadata-only read-back. For move-folder they require a matching approval token, explicit confirmation, exact opaque directory handle, exact opaque target parent handle, expected directory metadata SHA-256, metadata drift refusal, descendant-parent refusal, no-overwrite target proof, source/target presence proof, non-empty child preservation, `non_empty_allowed:true`, and metadata-only read-back with no content or child listing. For copy-folder they require a matching approval token, explicit confirmation, exact opaque directory handle, exact opaque target parent handle, expected directory metadata SHA-256, private bounded source-tree binding, metadata/tree drift refusal, hidden/symlink/package/tree-size refusal, descendant-parent refusal, no-overwrite target proof, source preservation proof, target presence proof, `non_empty_allowed:true`, no child listing, and metadata-only read-back. For create-text they require a matching approval token from the plan fingerprint, explicit confirmation, exact opaque parent folder handle, exclusive create, and read-back verification. For append-text they require a matching approval token, explicit confirmation, exact opaque file handle, expected current content SHA-256, drift refusal, bounded UTF-8 append, and read-back hash verification. For replace-text they require a matching approval token, explicit confirmation, exact opaque file handle, expected current content SHA-256, drift refusal, bounded UTF-8 replacement, same-directory atomic replace, and read-back hash verification. For trash-text they require a matching approval token, explicit confirmation, exact opaque file handle, expected current content SHA-256, drift refusal, supported text-file validation, fd-based no-follow atomic swap into recoverable Trash, post-swap SHA verification with swap-back on drift, no permanent unlink of the selected file, and original-handle absence proof. For delete-text they require a matching approval token, explicit confirmation, one exact supported text-file handle, expected current SHA-256, approval fingerprint binding to exact file identity, stale identity/token replay refusal, current-content drift refusal, no-follow/package/symlink traversal refusal, random-only hidden staging identity proof, permanent unlink, original absence proof, `verified_absent:true`, `permanently_deleted:true` only on successful removal, `trash_path_returned:false`, `staging_path_returned:false`, `content_text_returned:false`, `content_hash_returned:false`, and no raw path return. For rename-text/copy-text/move-text they require a matching approval token, explicit confirmation, exact opaque file handle, expected current content SHA-256, no-overwrite target validation, supported text-file validation, fd-based no-follow operations, source/target presence proof, and target content-hash read-back without content text. For rename-file/copy-file/move-file they require a matching approval token, explicit confirmation, one exact non-text non-package regular-file handle, expected file metadata SHA-256, metadata drift refusal, no-overwrite target proof, source/target presence proof, metadata-only read-back, `content_text_returned:false`, `content_hash_returned:false`, no content hash return, and no raw path return. For import-file they require a matching approval token, explicit confirmation, one caller-selected local non-text non-package regular file outside the configured iCloud Drive root, one exact target parent handle, private source identity/content binding, no-overwrite target proof, source preservation proof, metadata-only target read-back, `source_path_returned:false`, `source_hash_returned:false`, `content_text_returned:false`, `content_hash_returned:false`, no source path/hash return, no content hash return, and no raw path return. For replace-file they require a matching approval token, explicit confirmation, one exact non-text non-package regular-file handle, one caller-selected local non-text non-package source outside the configured iCloud Drive root, expected target metadata SHA-256, private source identity/content binding, extension match, target metadata drift refusal, source preservation proof, byte-replacement proof, metadata-only target read-back, `source_path_returned:false`, `source_hash_returned:false`, `content_text_returned:false`, `content_hash_returned:false`, no source path/hash return, no content hash return, and no raw path return. For trash-file they require a matching approval token, explicit confirmation, one exact non-text non-package regular-file handle, expected target metadata SHA-256, target metadata drift refusal, no-follow/package/symlink traversal refusal, recoverable Trash move, original absence proof, metadata-only read-back, `trash_path_returned:false`, `content_text_returned:false`, `content_hash_returned:false`, no content hash return, and no raw path return. For delete-file they require a matching approval token, explicit confirmation, one exact non-text non-package regular-file handle, expected target metadata SHA-256, target metadata drift refusal, no-follow/package/symlink traversal refusal, hidden staging identity proof, permanent unlink, original absence proof, metadata-only read-back, `staging_path_returned:false`, `trash_path_returned:false`, `content_text_returned:false`, `content_hash_returned:false`, no content hash return, and no raw path return. MCP annotations mark `icloud_drive_apply_change` as non-read-only, destructive, non-idempotent, and closed-world because the same static tool can replace existing file contents, move one exact folder to recoverable Trash, permanently delete one exact selected folder, rename or move one exact folder, copy one exact selected folder, move one exact text file to Trash, permanently delete one exact text file, rename, copy, or move one exact text file, rename/copy/move one exact regular file, import one caller-selected local regular file, replace one exact regular file, move one exact regular file to Trash, or permanently delete one exact regular file.

iCloud Drive exact folder move, including non-empty directories, through the plan/apply/read-back metadata-proof contract in `docs/V1_62_ICLOUD_DRIVE_FOLDER_MOVE_WRITE_DESIGN.md` and `docs/V1_145_ICLOUD_DRIVE_NON_EMPTY_FOLDER_RENAME_MOVE_WRITE_DESIGN.md`. For move-folder they require a matching approval token, explicit confirmation, exact opaque directory handle, exact opaque target parent handle, expected directory metadata SHA-256, metadata drift refusal, descendant-parent refusal, no-overwrite target proof, source/target presence proof, `non_empty_allowed:true`, and metadata-only read-back.

The current `calendar apply` CLI command and `calendar_apply_change` MCP tool are mutating tools. For create they require a matching approval token from the plan fingerprint, explicit confirmation, explicit target calendar title or exact `calendar:calendar:v1:` handle, explicit timed/all-day inputs or paired date-only start/end values that infer all-day state, optional exact relative alarm offsets or exact absolute display-alarm dates plus optional exact audio alarm sound names, optional exact email alarm addresses, or one exact structured geofence alarm, optional explicit timed-event IANA time zone, optional simple count-, end-date-, or explicit-unbounded daily/weekly/monthly/yearly recurrence with `recurrence_frequency`, `recurrence_interval`, exactly one of `recurrence_count`, `recurrence_end_date`, or `recurrence_unbounded:true`, and optional `recurrence_weekdays` for weekly recurrence, monthly recurrence, or yearly week-of-year recurrence, monthly-only `recurrence_month_days`, monthly-only `recurrence_month_weekdays`, `recurrence_year_month_days` only with `recurrence_year_months`, or exactly one of yearly-only `recurrence_year_months`, `recurrence_year_days`, or `recurrence_year_weeks` with `recurrence_weekdays` required for `recurrence_year_weeks`, and optional `recurrence_set_positions` requiring one of the selector arrays above, optional bounded `structured_location`, EventKit apply, and read-back verification. Explicit default-calendar create planning is plan-only: apply must use the resolved exact `calendar_handle` from the approved preview and rejects `use_default_calendar` before EventKit access. For recurrence create they require a matching approval token, explicit confirmation, `recurrence_frequency` of daily, weekly, monthly, or yearly, optional `recurrence_weekdays` only for weekly recurrence, monthly recurrence, or yearly week-of-year recurrence, optional `recurrence_month_days` only for monthly recurrence, optional `recurrence_month_weekdays` only for monthly recurrence and never mixed with `recurrence_month_days`, optional exactly one of `recurrence_year_months`, `recurrence_year_days`, or `recurrence_year_weeks` only when recurrence is yearly, `recurrence_year_weeks` only with exact `recurrence_weekdays`, `recurrence_interval` from 1 through 4, exactly one recurrence bound using either `recurrence_count` from 2 through 52, timezone `recurrence_end_date` within 3650 days of `start_date`, or explicit `recurrence_unbounded:true`, EventKit apply, and read-back recurrence metadata. For structured event location create/update they require one bounded title plus optional paired latitude/longitude and radius, conflict refusal against plain `location`, EventKit `EKStructuredLocation` apply, and read-back structured-location proof. For structured event location clearing they require update-only `clear_structured_location:true`, no `structured_location`, empty proposed `location`, exact `expected_structured_location`, a matching approval token, explicit confirmation, EventKit apply, and read-back absence proof with `structured_location_present:false`, `location_present:false`, and `structured_location_cleared_verified:true`. For audio alarms they require relative or absolute alarm triggers, a bounded bare `alarm_sound_name`, optional update/delete `expected_alarm_sound_name`, EventKit `EKAlarm.soundName` apply, and read-back sound-name proof. For email alarms they require relative or absolute alarm triggers, a bounded `alarm_email_address`, optional update/delete `expected_alarm_email_address_sha256`, EventKit `EKAlarm.emailAddress` apply, hash-only preview and read-back proof, and no raw email address output. For structured geofence alarms they require exactly one `alarm_proximity` of enter or leave, one bounded `alarm_structured_location` object with title plus optional paired latitude/longitude and radius, no relative/absolute/audio/email alarm co-mutation, optional update/delete `expected_alarm_proximity` and `expected_alarm_structured_location` binding, EventKit `EKAlarm.proximity` plus `EKAlarm.structuredLocation` apply, and read-back geofence proof. For update they require an exact `calendar:event:v1:` handle, expected current title/calendar/start/end/location/structured-location/notes/all-day/relative-or-absolute display/audio/email/geofence alarm/time-zone state or paired date-only expected/proposed values that infer all-day state, optional exact target `calendar:calendar:v1:` handle for calendar move, optional simple count-, end-date-, or explicit-unbounded recurrence add-to-non-recurring-event update including `recurrence_weekdays` for weekly recurrence, monthly recurrence, or yearly week-of-year recurrence, monthly-only `recurrence_month_days`, monthly-only `recurrence_month_weekdays`, `recurrence_year_month_days` only with `recurrence_year_months`, or exactly one of yearly-only `recurrence_year_months`, `recurrence_year_days`, or `recurrence_year_weeks` with `recurrence_weekdays` required for `recurrence_year_weeks`, and optional `recurrence_set_positions` requiring one of the selector arrays above, supported-event checks, stale-state refusal, EventKit apply to only the selected event, and read-back verification. For delete they require the same exact handle and expected current state including all-day, optional structured location, relative-or-absolute display/audio/email/geofence alarm, and optional time-zone state or paired date-only expected values that infer expected all-day state, refuse unsupported recurrence, attendee, mixed-alarm, unsupported multi-geofence, or procedure alarm state, remove only the resolved event, and require read-back absence proof. MCP annotations mark `calendar_apply_change` as non-read-only, destructive, non-idempotent, and closed-world because the same static tool can delete.

For yearly recurrence, month day-of-month selection is exact-gated through `recurrence_year_months` plus `recurrence_year_month_days`; month nth-weekday selection is exact-gated through `recurrence_year_months` plus `recurrence_year_month_weekdays`. These yearly month subselectors require an approved count, end-date, or explicit-unbounded recurrence bound, are bound into the approval fingerprint, and are never mixed with each other, `recurrence_year_days`, or `recurrence_year_weeks`.

The current `contacts apply` CLI command and `contacts_apply_change` MCP tool are mutating tools. For create-contact they require a matching approval token from the plan fingerprint, explicit confirmation, bounded create fields, optional exact `contacts:container:v1:` handle plus `container_safe_sha256`, Contacts.framework apply, and read-back verification. For exact update they require one exact `contacts:contact:v1:` handle, `update_safe_sha256` drift refusal over scalar, method, rich field, and image metadata, replacement only when the specific field is provided, omitted arrays preserved including omitted method-array preservation, provided arrays replaced including provided method-array replacement, explicit empty arrays cleared including explicit empty-array clears, Contacts.framework apply, and read-back verification. The exact note append/set/clear/merge contract remains designed and synthetic-testable, but the live helper fails closed with `contacts_note_unavailable` before mutation because the restricted Contacts-notes entitlement is unavailable. For exact group membership they require one exact contact handle plus one exact `contacts:group:v1:` handle, contact `update_safe_sha256`, group `group_safe_sha256`, Contacts.framework `CNSaveRequest.addMember(_:to:)` or `CNSaveRequest.removeMember(_:from:)`, and metadata-only group read-back. For exact group create they require bounded `group_name`, optional exact container handle plus `container_safe_sha256`, duplicate-name refusal in the selected container, Contacts.framework `CNSaveRequest.add(_:toContainerWithIdentifier:)`, and metadata-only group read-back. For exact group rename/delete they require one exact `contacts:group:v1:` handle, `group_safe_sha256`, Contacts.framework `CNSaveRequest.update(_:)` or `CNSaveRequest.delete(_:)`, and metadata-only group read-back or absence proof with `contacts_deleted:false`. For exact batch they require a capped list of approved existing-contact operation objects, bind each child approval fingerprint into one batch approval token, stop on first failure, and report partial state if any earlier item applied; group create/rename are not allowed inside batch. For exact delete they require one exact `contacts:contact:v1:` handle, `delete_safe_sha256` drift refusal, Contacts.framework delete, and read-back absence proof. MCP annotations mark `contacts_apply_change` as non-read-only, destructive, non-idempotent, and closed-world because the same static tool can delete one exact selected contact or one exact selected group.

The current `notes apply` CLI command and `notes_apply_change` MCP tool are mutating tools. For create-note they require a matching approval token from the plan fingerprint, explicit confirmation, Notes.app automation, and exact-content read-back verification. For create-folder they require a matching approval token from the plan fingerprint, explicit confirmation, one exact normal parent `notes:folder:v1:` handle, same-parent idempotency, Notes.app `make new folder at targetFolder` automation, and metadata-only read-back with `parent_folder_confirmed:true`, `folder_content_returned:false`, and `note_content_returned:false`. For rename-folder they require a matching approval token from the plan fingerprint, explicit confirmation, one exact normal folder `notes:folder:v1:` handle, expected folder-title SHA-256 binding, Notes.app `set name of targetFolder` automation scoped to the selected folder id, already-applied retry handling, and metadata-only read-back with `renamed:true`, `folder_content_returned:false`, and `note_content_returned:false`. For delete-folder they require a matching approval token, explicit confirmation, one exact empty normal child `notes:folder:v1:` handle, expected folder-title SHA-256 binding, non-empty/root/smart-folder refusal, Notes.app `delete targetFolder` automation scoped to the selected folder id, and absence read-back with `verified_absent:true`, `folder_content_returned:false`, and `note_content_returned:false`. For append-text they require a matching approval token, explicit confirmation, exact opaque note handle, expected current content SHA-256, drift refusal, bounded plaintext append, shared/locked-note refusal, and exact-content read-back hash verification. For replace-text they require the same exact note handle and expected current SHA-256, refuse locked/deleted/shared notes, replace only bounded plaintext, and verify the replacement through exact-content read-back. For move-to-folder they require the same exact note handle, exact opaque target folder handle, expected current SHA-256, same-account folder proof, shared/locked-note refusal, current-content drift refusal, Notes.app move automation, and folder read-back verification without returning note body text. For delete they require the same exact note handle and expected current SHA-256, refuse locked/deleted/shared notes, recheck current HTML before deletion, and verify absence read-back. MCP annotations mark `notes_apply_change` as non-read-only, destructive, non-idempotent, and closed-world because the same static tool can rename, move, or delete one exact selected Notes object.

The current `mail apply` CLI command and `mail_apply_change` MCP tool are mutating tools. For create-draft they require a matching approval token from the plan fingerprint, explicit confirmation, save-only Mail.app automation, and local Drafts read-back verification when the local Mail store exposes the saved draft. Create-draft may additionally accept an exact `mail:sender:v1:` sender handle. The approved attachment lane is caller-selected local file attachments for draft/send/reply/reply-all/forward. Create-draft, send-message, reply-message, reply-all-message, and forward-message may accept bounded caller-selected local file attachments; attachment plans bind local file identity, reject symlinks/directories/empty/oversized files, return no file paths or bytes, and apply requires Mail automation attachment-count confirmation before reporting success. For send-message they require bounded plaintext recipients, subject, body text, an irreversible-send plan fingerprint, explicit confirmation, scoped Mail.app send automation, no draft save, optional bounded caller-selected local attachments, and local Sent-copy read-back when the local Mail store indexes the sent message; apply output must not echo the sent body and a delayed Sent read-back returns `partial` instead of inviting blind retry. For reply-message they require an exact `mail:message:v2:` source handle, the local `.emlx` RFC Message-ID bridge, sender-only reply mode, bounded plaintext body text, an irreversible-send plan fingerprint, explicit confirmation, stale source-state refusal, account/nested-mailbox/Message-ID-scoped Mail.app reply automation, optional bounded caller-selected local attachments, no direct-recipient override, no draft save, and local Sent-copy read-back when indexed; apply output must not echo the reply body and a delayed Sent read-back returns `partial`. For reply-all-message they require the same exact source gate with reply-all mode, `reply to all true`, stale source-state refusal, optional bounded caller-selected local attachments, no direct-recipient override, no draft save, and local Sent-copy read-back when indexed; apply output must not echo the reply body and delayed Sent read-back returns `partial`. For forward-message they require an exact `mail:message:v2:` source handle, the local `.emlx` RFC Message-ID bridge, explicit recipients, non-empty bounded plaintext prepend text, default source attachment/non-body-part refusal unless `include_source_attachments` is explicitly set, an irreversible-send plan fingerprint, explicit confirmation, stale source-state and source-attachment/non-body-part-state refusal, account/nested-mailbox/Message-ID-scoped Mail.app forward automation, optional bounded caller-selected local attachments, no direct subject override, no draft save, exact Mail-derived total attachment-count confirmation, and exactly one local Sent-copy read-back when indexed; apply output must not echo the forward body, source content, source attachment paths, or source attachment bytes and delayed or ambiguous Sent read-back returns `partial`. For exact-message or capped exact bulk mark-read/mark-unread/flag-message/unflag-message/archive-message/move-message/trash-message they require `mail:message:v2:` handles, the local `.emlx` RFC Message-ID bridge, current read/flag/mailbox state fingerprints, explicit confirmation, stale-state refusal, account/nested-mailbox/Message-ID-scoped Mail.app automation, and local read-back verification. Bulk triage requires unique exact message handles, caps batches at 20, binds every selected handle/current state/target state into one approval fingerprint, preflights every selected message before mutation, skips already-satisfied messages without automation, and reports `partial` if a later item fails after an earlier item applied. Archive resolves only the selected message account's local Archive mailbox, move-message requires an exact non-Trash/Junk `mail:mailbox:v1:` target selected through mailbox metadata, supports exact cross-account targets only through `docs/V1_66_MAIL_CROSS_ACCOUNT_MOVE_WRITE_DESIGN.md`, returns opaque source/target account refs without raw account identifiers, Trash resolves only the selected message account's local Trash mailbox, and targets are rechecked before apply. MCP annotations mark `mail_apply_change` as non-read-only, destructive, non-idempotent, and closed-world because the same static tool can send external mail or move a message to Trash.

The current `photos apply` CLI command and `photos_apply_change` MCP tool are mutating tools. For import they require a matching approval token from the plan fingerprint, explicit confirmation, source-file hash binding, PhotoKit import, and created-asset read-back verification. For update_flags they require one exact `photos:asset:v1:` handle, expected favorite/hidden current-state binding, at least one target flag, matching approval token, explicit confirmation, PhotoKit property change, and selected-asset read-back verification without returning raw PhotoKit identifiers. For delete they require one exact `photos:asset:v1:` handle, current safe-state binding through `delete_safe_sha256`, matching approval token, explicit confirmation, PhotoKit `canPerform(.delete)` support, `PHAssetChangeRequest.deleteAssets`, and read-back absence proof with `verified_absent:true`, `recently_deleted_empty:false`, no raw PhotoKit identifier return, and no asset bytes. Photos exact regular-album membership add/remove through the plan/apply/read-back contract in `docs/V1_151_PHOTOS_ALBUM_MEMBERSHIP_WRITE_DESIGN.md`. For add_to_album/remove_from_album they require a matching approval token, explicit confirmation, one exact `photos:asset:v1:` handle, one exact `photos:album:v1:` handle, exact `expected_in_album` binding, regular-album add/remove support, PhotoKit `PHAssetCollectionChangeRequest`, `addAssets` or `removeAssets`, membership read-back proof, `raw_asset_identifier_returned:false`, and `raw_album_identifier_returned:false`. Photos exact regular-album create/rename/delete through the plan/apply/read-back contract in `docs/V1_154_PHOTOS_REGULAR_ALBUM_MANAGEMENT_WRITE_DESIGN.md`. For create_album/rename_album/delete_album they require a matching approval token, explicit confirmation, full Photos Library authorization, bounded non-empty title for create/rename, exact `photos:album:v1:` handle for rename/delete, expected album-state binding, duplicate-title refusal, empty-album proof for delete, public PhotoKit `PHAssetCollectionChangeRequest`, title read-back for create/rename, absence proof for delete, and `raw_album_identifier_returned:false`. MCP annotations mark `photos_apply_change` as non-read-only, destructive, non-idempotent, and closed-world because the same static tool can delete one exact selected Photos asset or mutate exact regular-album membership or exact regular-album state.

The current `messages apply` CLI command and `messages_apply_change` MCP tool are mutating tools. For send-text they require a matching approval token from the plan fingerprint, explicit confirmation, exact existing chat handle, stale chat-state refusal, Messages.app automation, ghost-row detection, and local `chat.db` read-back verification without returning the sent body text in apply output. For send-file they require the same exact chat gate plus local regular file validation, file-identity binding, stale chat/file-state refusal, Messages.app automation, and local outgoing attachment read-back without returning file bytes or local paths. MCP annotations mark `messages_apply_change` as non-read-only, non-destructive, idempotent, and closed-world.

Calendar selected recurring occurrence event URL set/clear through the plan/apply/read-back contract in `docs/V1_117_CALENDAR_SELECTED_OCCURRENCE_EVENT_URL_WRITE_DESIGN.md`.
For selected recurring occurrence event URL set/clear they require update-only `recurrence_update_scope:this_event`, exact recurrence shape binding, selected occurrence start/end identity binding, adjacent occurrence identity and hash-only URL-state binding, exact allow-listed `event_url` or update-only `clear_event_url:true`, required URL expected-state binding for clear, a matching approval token, explicit confirmation, EventKit `.thisEvent` save, selected-occurrence hash-only event URL read-back or absence proof, no raw URL return, and adjacent-occurrence presence/recurrence/URL-state preservation proof.

Calendar selected recurring occurrence structured location set/clear through the plan/apply/read-back contract in `docs/V1_118_CALENDAR_SELECTED_OCCURRENCE_STRUCTURED_LOCATION_WRITE_DESIGN.md`.
For selected recurring occurrence structured location set/clear they require update-only `recurrence_update_scope:this_event`, exact recurrence shape binding, selected occurrence start/end identity binding, adjacent occurrence identity plus hash-only sibling URL/plain-location/structured-location/alarm-state binding, bounded `structured_location` with explicit expected structured-location absence or exact `expected_structured_location` replacement binding, update-only `clear_structured_location:true` with exact `expected_structured_location`, a matching approval token, explicit confirmation, EventKit `.thisEvent` save, selected-occurrence structured-location read-back or structured/plain-location absence proof, and adjacent-occurrence presence/recurrence/URL/plain-location/structured-location/alarm-state preservation proof.

Calendar selected recurring occurrence all-day set/clear/date-only reschedule through the plan/apply/read-back contract in `docs/V1_121_CALENDAR_SELECTED_OCCURRENCE_ALL_DAY_WRITE_DESIGN.md`.
For selected recurring occurrence all-day set/clear/date-only reschedule they require update-only `recurrence_update_scope:this_event`, exact recurrence shape binding, selected occurrence start/end identity binding, adjacent occurrence identity plus hash-only sibling URL/plain-location/structured-location/alarm-state binding, exact expected all-day/time-zone state binding, date-only proposed start/end for all-day set or same-state all-day date-only reschedule, explicit proposed time zone for all-day-to-timed clear, a matching approval token, explicit confirmation, EventKit `.thisEvent` save, selected-occurrence all-day read-back proof, and adjacent-occurrence presence/recurrence/URL/plain-location/structured-location/alarm-state preservation proof.

For mid-series recurrence replacement they require update-only `recurrence_update_scope:future_events`, no scalar, calendar move, URL, alarm, availability, structured-location, or clear-recurrence co-mutation, exact expected recurrence shape binding, exact approved replacement recurrence binding, selected occurrence start/end identity binding, previous occurrence identity binding, future occurrence identity binding, a matching approval token, explicit confirmation, EventKit `.futureEvents` save with a new `EKRecurrenceRule`, selected-occurrence replacement recurrence read-back, future occurrence replacement recurrence proof, original future-slot absent-or-replaced proof, and previous-occurrence preservation proof.

For future-series recurring-event scalar update they require update-only `recurrence_update_scope:future_events` without recurrence fields, no recurrence clear/replacement, calendar move, URL, alarm, availability, structured-location, time, time-zone, or all-day co-mutation, exact expected recurrence shape binding, selected occurrence start/end identity binding, previous occurrence identity binding, future occurrence identity binding, a matching approval token, explicit confirmation, EventKit `.futureEvents` save, selected-occurrence scalar read-back, future-occurrence scalar read-back, and previous-occurrence preservation proof.

## First Candidate Write Surfaces

| Surface | Candidate operations | Preferred API | Extra approval checks |
| --- | --- | --- | --- |
| Reminders | Create reminder, complete reminder, uncomplete reminder, update due date/title/notes/priority, move one exact reminder to one exact same-source target list, delete one exact reminder, and exact list create/rename/empty-delete/same-source migrate-delete | EventKit helper | Approved. Confirm target list/title/due date or exact reminder handle plus expected title, exact expected current-list handle, exact same-source target-list handle plus expected current list name for list-move, expected completed state for delete/list-move, expected notes SHA-256 for notes replacement and delete, expected priority for priority replacement and delete, approval token, explicit confirmation, and `target_list_verified:true` identity proof before list-move success; confirm exact source/target `reminders:list:eventkit:v1:` handles for list management, source/target safe-hash binding, empty-list proof for rename/delete, bounded same-source migration count proof for migrate-delete, source-empty proof, target-count proof, and source-list absence proof; no non-empty list delete outside bounded same-source migrate-delete, cross-source migrate-delete, bulk list operations, or list/account management outside exact same-source list-move plus exact list create/rename/empty-delete/same-source migrate-delete |
| Calendar | Create timed or all-day event with exact relative alarm offsets, exact absolute display alarms, exact audio alarms, or exact structured geofence alarms, optional explicit timed-event IANA time zone, optional explicit availability, optional exact allow-listed event URL, optional structured location, date-only all-day inference, optional explicit default-calendar create planning to an exact handle, and optional simple count-, end-date-, or explicit-unbounded daily/weekly/monthly/yearly recurrence including weekly weekday, monthly weekday, monthly day-of-month, monthly nth-weekday, and yearly month/month-day/month-nth-weekday/day-of-year/week-of-year plus explicit weekday selection for week-of-year and selector-backed set-position filtering; update title/time/location/structured-location/notes/all-day/relative-or-absolute display/audio/email/geofence alarm/time-zone/availability/event URL state including update-only URL clearing plus simple recurrence add-to-non-recurring-event with optional weekly weekdays, monthly month days, monthly nth-weekdays, or yearly months, year-month nth-weekdays, day-of-year values, or week-of-year values for one exact event; update one selected recurring occurrence title/plain-location/notes, timed start/end/time-zone, availability, allow-listed event URL set/clear, structured location set/clear, display-alarm set/clear, action-alarm set/clear, all-day set/clear/date-only reschedule, or target-calendar move through `recurrence_update_scope:this_event`; delete one exact event, one selected recurring occurrence, one future-event recurring span, or one whole recurring series from the first visible occurrence; clear recurrence from the first visible occurrence or from a selected mid-series occurrence and future occurrences of a supported recurring series; replace recurrence for a selected mid-series occurrence and future occurrences with a supported finite recurrence; update title/plain-location/notes, timed start/end/time-zone, availability, event URL set/clear, structured location set/clear, display-alarm set/clear, action-alarm set/clear, all-day set/clear/date-only reschedule, or target-calendar move state for a selected occurrence and all future occurrences through `recurrence_update_scope:future_events` | EventKit helper | Approved. Confirm exact calendar title or `calendar:calendar:v1:` handle plus explicit `all_day` or paired date-only start/end values, optional `alarm_offsets_minutes`, `alarm_absolute_dates`, or `alarm_proximity` plus optional `alarm_sound_name` and required `alarm_structured_location` for geofence alarms, optional timed-event `time_zone`, optional `availability`, optional allow-listed `event_url`, optional `structured_location` with title plus optional paired coordinates/radius, and optional `recurrence_frequency`/`recurrence_interval` plus exactly one of `recurrence_count`, `recurrence_end_date`, or `recurrence_unbounded:true` plus `recurrence_weekdays` for weekly recurrence, monthly recurrence, or yearly week-of-year recurrence, monthly-only `recurrence_month_days`, monthly-only `recurrence_month_weekdays`, or yearly-only `recurrence_year_months`, `recurrence_year_month_weekdays`, `recurrence_year_days`, or `recurrence_year_weeks` for create or add-to-non-recurring-event update; `recurrence_year_month_weekdays` requires `recurrence_year_months`; `use_default_calendar` is plan-only and apply must use the resolved exact `calendar_handle`; or exact event handle plus expected current title/calendar/start/end/location/structured-location/notes/all-day/relative-or-absolute display/audio/email/geofence alarm/optional time-zone state, required `expected_availability` when changing availability, optional `expected_structured_location` when binding structured-location state, expected event URL presence/hash when a current URL exists, update-only `clear_event_url` with exact URL hash binding, optional target `calendar:calendar:v1:` handle only for ordinary exact-event update/move, update-only `clear_recurrence` with exact recurrence/occurrence binding plus future absence and previous absence-or-preservation proof, update-only `recurrence_update_scope:future_events` with replacement recurrence fields, exact previous/selected/future occurrence identity, selected/future replacement proof, original future-slot absent-or-replaced proof, previous preservation proof, and no target-calendar co-mutation, or update-only `recurrence_update_scope:future_events` without recurrence fields for future-series title/plain-location/notes, timed reschedule, availability, or event URL set/clear, exact previous/selected/future occurrence identity, selected/future recurrence-shape plus scalar/timed/availability/hash-only URL proof, previous preservation proof, original selected/future slot absence-or-approved-replacement proof when dates move, required expected availability for availability changes, required URL expected-state binding for URL clear, and no all-day/calendar/alarm/structured-location co-mutation; or update-only `recurrence_update_scope:this_event` with exact recurring occurrence handle, expected current title/start/end/plain location/notes/availability/event URL/structured-location/alarm/all-day/time-zone state as needed, selected-occurrence identity, adjacent-occurrence identity plus hash-only sibling URL/plain-location/structured-location/alarm-state binding and structured-location absence or exact expected structured-location binding for set/clear, EventKit `.thisEvent` save, selected-occurrence read-back proof, and adjacent-occurrence presence/recurrence/URL/plain-location/structured-location/alarm-state preservation proof; or the same expected state for delete, optional delete-only `recurrence_delete_scope:this_event` with recurrence-presence binding plus selected-occurrence and adjacent-occurrence identity proof for selected recurring occurrence delete, optional delete-only `recurrence_delete_scope:future_events` with recurrence-presence binding plus selected, previous, and future occurrence identity proof for selected-and-future recurring span delete, or optional delete-only `recurrence_delete_scope:all_events` with recurrence-presence binding plus selected occurrence identity, future occurrence identity, and bounded previous-occurrence absence proof for whole-series recurring-event delete, approval token, explicit confirmation, selected-occurrence absence proof, adjacent-occurrence preservation proof for `this_event`, selected/future absence plus previous preservation proof for `future_events`, and selected/future absence plus bounded previous absence proof for `all_events`; no delete outside the exact-event gate, recurrence outside simple count-, end-date-, or explicit-unbounded create, add-to-non-recurring-event update, weekly weekday selection, monthly weekday selection, monthly day-of-month selection, monthly nth-weekday selection, yearly month/month-day/month-nth-weekday/day-of-year/week-of-year plus explicit weekday selection for week-of-year and selector-backed set-position filtering, selected-occurrence title/plain-location/notes/timed reschedule/availability/event URL set/clear/structured-location set/clear/display-alarm set/clear/action-alarm set/clear/all-day set/clear/date-only reschedule/target-calendar move update, future-series title/plain-location/notes/timed reschedule/availability/event URL set/clear/structured-location set/clear/display-alarm set/clear/action-alarm set/clear/all-day set/clear/date-only reschedule/target-calendar move update, selected-occurrence delete, future-event recurring span delete, or whole-series recurring-event delete, attendees, silent default-calendar guessing or default-calendar mutation, timed-event time-zone inference, availability outside explicit support-mask validation, non-allow-listed event URL schemes, procedure alarms, or bulk operations |
| Notes | Read one exact note's rich-text HTML body plus extracted visible text (`content_format:html`, bounded, exact-handle); create note in the default folder or one exact selected folder; create one rich-text note from sanitized HTML in the default folder or one exact selected folder; create one exact child folder under one selected normal parent folder; rename one exact normal folder; delete one exact empty normal child folder; move one exact empty normal child folder to one exact same-account destination parent; append text to an exact note; replace plaintext for an exact note; replace one exact note's body with sanitized rich-text HTML bound to its expected extracted visible-text SHA-256; move one exact note to one exact folder; delete one exact note | Notes.app automation | Approved. Confirm title/body and optional exact `notes:folder:v1:` folder handle for note create, title plus sanitized `body_html` and optional exact folder handle for rich-text body create, exact parent `notes:folder:v1:` handle and folder title for child-folder create, exact normal folder `notes:folder:v1:` handle plus expected folder-title SHA-256 and new title for folder rename, exact empty child folder handle plus expected folder-title SHA-256 for delete-folder, exact empty source folder handle plus exact same-account destination-parent handle, expected folder-title SHA-256, source metadata-state binding, and destination-parent proof for move-folder, exact note handle plus expected current SHA-256 for append/replace/delete, exact note handle plus sanitized `body_html` plus expected current extracted visible-text SHA-256 for rich-text body replace, or exact note handle plus exact target folder handle plus expected current SHA-256 for move, approval token, and explicit confirmation before apply; rich-text body create/replace sanitize input HTML (rejecting NUL/C0 control characters, `<script>`, `on*` handlers, and `javascript:`/`vbscript:`/`data:` URIs) and prove a semantic (extracted visible-text) read-back; no note delete outside the exact-note delete gate, folder delete outside the exact empty child-folder gate, move outside the exact-note/exact-folder move-to-folder or exact empty child-folder move gates, update outside the plaintext append/replace and rich-text body create/replace gates, broad body search, attachment mutation, broad attachment export, folder/account targeting outside exact note create/child-folder create/folder-rename/delete-folder/folder-move/note-move gates, root/recursive folder delete, root/non-empty/recursive/cross-account folder move, root/default-account folder creation, checklist state, locked/shared-note mutation, Recently Deleted management, or bulk operations |
| Mail | Create draft with optional exact sender selection and optional local file attachments; send bounded plaintext message with optional local attachments; exact-message sender-only reply with optional local attachments; exact-message reply-all with optional local attachments; exact-message forward with optional local attachments and optional source attachment-like part preservation; exact-message mark read/unread; exact-message flag/unflag; exact-message archive; exact-message target-mailbox move; exact-message move-to-Trash; synthetic `LAD-TEST-*` mailbox create/rename; source-gated synthetic mailbox delete/cleanup only when public Mail.app deletion plus exact target-state binding, mailbox-scoped absence proof, and Mail-idle guards succeed | Mail.app automation plus local `.emlx` RFC Message-ID bridge | Approved. Confirm recipients, subject, body length/preview, optional exact `mail:sender:v1:` handle for draft/send/reply/reply-all/forward, optional bounded caller-selected local attachment paths for create-draft/send/reply/reply-all/forward, approval token, explicit confirmation, save-only behavior for drafts, selected-sender read-back when a sender handle is used, attachment-count confirmation when attachments are selected, irreversible-send metadata plus no body echo for send; confirm exact source message handle, sender-only or reply-all mode, bounded body preview, approval token, explicit confirmation, stale-state refusal, no direct-recipient override, attachment-count confirmation when local attachments are selected, and Sent-copy read-back for reply/reply-all; confirm exact source message handle, explicit recipients, bounded prepend text, default source attachment/non-body refusal unless `include_source_attachments` is set, optional caller-selected local attachments, stale source-state refusal, no subject override, pre-send Mail attachment-count verification for source attachment-like parts, and Sent-copy read-back for forward without per-part Sent identity/content proof; confirm exact message handle, current read/flag/mailbox state, approval token, explicit confirmation, stale-state refusal, same-account Archive/Trash target resolution and exact target-mailbox resolution including cross-account exact targets, no permanent deletion through the triage gate, and read-back for read/flag/archive/move/trash triage; confirm exact sender handle or exact mailbox handle, top-level `LAD-TEST-*` mailbox names, empty mailbox state, approval token, explicit confirmation, and create/rename read-back for synthetic mailbox management; confirm exact `LAD-TEST-*` Trash/Junk message handles or an exact sender handle for synthetic-only special-mailbox cleanup, approval token, explicit confirmation, synthetic-only recheck, exact target-state binding, and absence proof before reporting cleanup success; no send outside the approved send-message gate, reply outside the approved exact-message sender-only/reply-all gates, forward outside the approved exact-message gate, sender selection outside the approved draft/send/reply/reply-all/forward sender gate, attachments outside the approved draft/send/reply/reply-all/forward local-file gate, Mail source attachment/non-body-part forwarding outside explicit `include_source_attachments`, broad attachment export, move outside the approved exact target-mailbox gate, Trash/Junk-class move target, permanent delete outside synthetic cleanup, flag mutation outside approved exact-message flag/unflag, real account management, or non-synthetic mailbox management |
| Contacts | Create contact; exact scalar/method/rich-field/image update; exact group membership; exact group create/rename/delete; exact batch; exact-contact delete | Contacts.framework helper | Approved live for the listed non-note operations. Confirm exact handles, current-state hashes, matching approval token, explicit confirmation, and operation-specific read-back or absence proof. Free-form labels are accepted up to 255 characters; control characters and oversize labels are refused. Exact note append/set/clear/merge remains a designed, synthetic-testable contract but is live-unavailable and fails closed with `contacts_note_unavailable` before mutation. No raw identifier input, broad contact dumps in chat, direct database writes, duplicate merge automation, or complete Contacts management. |
| Photos | Import one image or video asset; exact asset favorite/hidden update; exact selected-asset delete; exact regular-album membership add/remove; exact regular-album create/rename/delete | PhotoKit change requests | Approved. Confirm caller-selected source file, inferred media type, file size/hash, approval token, and explicit confirmation before import apply; confirm exact asset handle, expected favorite/hidden state, target favorite/hidden state, approval token, and explicit confirmation before update_flags apply; confirm exact `photos:asset:v1:` handle, exact `photos:album:v1:` handle, `expected_in_album`, regular-album support, approval token, explicit confirmation, and membership read-back before album add/remove success; confirm full Photos Library authorization, bounded non-empty title for create/rename, exact `photos:album:v1:` handle and expected album state for rename/delete, duplicate-title refusal, empty-album proof for delete, approval token, explicit confirmation, title read-back for create/rename, and absence proof for delete; no content edits, permanent delete/Recently Deleted empty, smart/shared/synced album targeting, metadata mutation outside favorite/hidden, network fetch, or bulk operations |
| Messages | Send plaintext or one local file to one exact existing chat | Messages.app automation plus local `chat.db` read-back | Approved. Confirm exact chat handle, body length/preview for text or file metadata/identity for file, approval token, explicit confirmation, stale chat/file-state refusal, and ghost-row detection before success; no direct-recipient send, new chat, SMS fallback selection, reactions, edit, delete, account selection, or file-send outside the approved exact-chat send-file gate |
| Safari | None | Local `Bookmarks.plist` is read-only in the current release | Bookmark/Reading List mutation, history/tabs access, cookies, passwords, page content, and browser state require separate design and approval |
| Shortcuts | Run one exact identifier-bound shortcut | Apple `shortcuts` CLI through argv-only invocation | Approved only through exact `shortcuts:item:v1:` plan/apply, resolved-identifier approval-fingerprint binding, matching token, explicit confirmation, bounded NUL-rejected input, and a hard timeout. Success proves invocation of the named shortcut only; arbitrary shortcut side effects are not read back. Name-resolved or unbounded runs, open/view/sign/export, body/action-graph reads, dynamic run tools, import/create/update/delete/duplicate, and all other mutation remain blocked. |
| Books | None | Local Books SQLite stores are read-only in the current release | Book/chapter/PDF/EPUB text extraction, broad library or annotation dumps/searches, iCloud fetch, Books.app automation, and mutation require separate design and approval |
| Podcasts | None | Local Podcasts SQLite store is read-only in the current release | Transcript/audio/feed/enclosure URL extraction, broad library or episode-description dumps/searches, iCloud media fetch, Podcasts.app automation, and mutation require separate design and approval |
| Music | None | Music.app automation is used only for bounded track/playlist metadata in the current release | Audio/lyrics/file-path export, play history, ratings/favorites, playlist track dumps, playback/queue control, raw database parsing, library import/delete, iCloud media fetch, and mutation require separate design and approval |
| TV | None | TV.app automation is used only for bounded item/playlist metadata in the current release | Video/file-path/artwork/description export, playback or watched state, ratings/favorites, playlist item dumps, playback/queue control, raw library parsing, library import/delete, iCloud media fetch, and mutation require separate design and approval |
| Freeform | None | Local Freeform SQLite store is read-only in the current release | Board BLOB/CRDT decoding, board title/content export, decoded board item dumps, asset export, previews, collaboration payloads, broad dumps, raw database parsing, Freeform.app automation, and mutation require separate design and approval |
| Hide My Email | None | No approved local public API | Authoritative inventory or mutation requires a new source review and explicit approval |
| iCloud Drive | Create one child folder or a bounded one-to-three-component folder path; rename/move/trash/copy one exact folder, and delete one exact selected folder; create/append/replace/trash/delete/rename/copy/move one exact text file; rename/copy/move one exact regular file; import one caller-selected regular file; replace, trash, or permanently delete one exact regular file | Local filesystem | Approved. Confirm exact parent folder plus stable parent identity and one folder name or one to three `folder_components`, exact folder, exact target parent folder, exact text file, or exact non-text non-package regular file by opaque handle, filename, expected current content SHA-256, or expected metadata SHA-256, approval token, and explicit confirmation before apply. Folder path create uses fd-based no-follow `mkdir` per component, treats existing directories as idempotent, reports partial if mutation starts before a later failure, and returns final metadata only. Folder copy requires private bounded source-tree binding, refuses hidden/symlink/package trees, preserves source, and returns no content/hash/path or child listing. Regular-file rename/copy/move/import/replace/trash/delete returns metadata-only proof with `content_text_returned:false` and `content_hash_returned:false`; import and replace also return `source_path_returned:false` and `source_hash_returned:false`; trash and delete return raw-path flags false. No file permanent-delete outside the exact delete-text or delete-file gates, unbounded recursive folder writes/deletes, unbounded folder copy, folder copy outside the exact selected-folder copy gate, import outside the exact import-file gate, replace outside the exact replace-file gate, trash outside the exact trash-text, exact folder trash, or exact regular-file trash gates, rename/copy/move outside exact text-file or regular-file gates, or binary/document content generation, regular-file mutation outside exact import-file, exact replace-file, exact trash-file, exact delete-file, or metadata-only rename/copy/move gates. |

## Default Refusals

Outside the approved Reminders, iCloud Drive, Calendar, live-available Contacts operations, Notes, Mail, Photos, Messages, bounded home-directory Filesystem, and exact identifier-bound Shortcuts run apply gates, the plugin must refuse:

- Sending mail outside the approved send-message gate.
- Messages direct-recipient sends, new chat creation, SMS fallback selection, file-send outside the approved exact-chat send-file gate, rich text, effects, inline replies, reactions/tapbacks, edit, unsend, delete, mark read, group management, participant lookup for mutation, contact lookup, broad text search, and account selection; read-only participant metadata is limited to `docs/V1_64_MESSAGES_PARTICIPANTS_METADATA.md`, and current risky-mutation source-review evidence is recorded in `docs/V1_47_MESSAGES_RISKY_MUTATION_SOURCE_REVIEW.md`.
- Mail reply outside the approved exact-message sender-only or reply-all gates, forward outside the approved exact-message forward-message gate, sender selection outside the approved draft/send/reply/reply-all/forward sender gate, attachments outside the approved draft/send/reply/reply-all/forward local-file attachment gates, Mail source attachment/non-body-part forwarding outside explicit `include_source_attachments`, cross-account move outside the approved exact target-mailbox gate, Trash/Junk-class target through move-message, permanent delete outside synthetic `LAD-TEST-*` cleanup, empty Trash/Junk outside synthetic-only cleanup, flag mutation outside approved exact-message flag/unflag, read/flag/archive/move/trash triage outside approved mark-read/mark-unread/flag-message/unflag-message/archive-message/move-message/trash-message, mailbox management outside empty top-level `LAD-TEST-*` mailboxes, account management, broad attachment export, HTML/rich-text draft mutation, template mutation outside plugin-managed local templates, query-result triage outside capped preview-to-exact-plan flow, or unbounded bulk mail mutation.
- Deleting, archiving, moving, or marking Messages beyond the approved send-text/send-file apply gate; broad Messages attachment export; Messages attachment mutation outside the approved exact-chat send-file gate; inline Messages attachment bytes; source Messages media paths.
- Creating, deleting, deactivating, or managing Hide My Email aliases.
- Deleting Contacts outside the approved exact-contact delete gate; deleting Photos or Voice Memos; deleting iCloud Drive files outside the exact iCloud Drive trash-text/delete-text/trash-file/delete-file gates; emptying Trash for iCloud Drive files; deleting Notes outside the approved exact-note absence-proof gate; deleting Calendar events outside the approved exact-event absence-proof gate; deleting Reminders outside the approved exact-handle delete gate.
- Photos edit, smart/shared/synced album targeting, metadata mutation outside favorite/hidden, thumbnails, inline asset bytes, network iCloud fetch, importing from URLs, bulk Photos operations, and album membership outside exact regular-album add/remove gates.
- Safari bookmark creation/update/delete/move, history access, open tabs/iCloud tabs, private browsing data, cookies, passwords, browser caches, page content, or Safari UI automation.
- Shortcuts runs outside the approved exact-handle plan/apply gate; shortcut open/view/sign/export, body/action-graph reads, dynamic run tools, folder-scoped or name-resolved execution, Shortcuts SQLite scraping, import, create, update, delete, duplicate, or other mutation.
- Books book/chapter/PDF/EPUB text extraction, broad library dumps, broad annotation search/dumps, raw Books IDs/paths, iCloud content fetch, Books.app automation, or mutation.
- Podcasts transcript/audio/feed/enclosure URL extraction, broad library dumps, broad episode-description search/dumps, raw Podcasts IDs/paths, iCloud media fetch, Podcasts.app automation, or mutation.
- Music audio/lyrics/file-path export, play history, ratings/favorites, playlist track dumps, playback/queue control, raw Music database parsing, library import/delete, iCloud media fetch, Music.app mutation, or broad library dumps.
- TV video/file-path/artwork/description export, playback or watched state, ratings/favorites, playlist item dumps, playback/queue control, raw TV library parsing, library import/delete, iCloud media fetch, TV.app mutation, or broad library dumps.
- Freeform board BLOB/CRDT decoding, board title/content export, decoded board item dumps, asset export, previews, collaboration payloads, broad dumps/searches, raw Freeform database parsing, Freeform.app automation, or mutation.
- Notes update outside approved exact-note plaintext append/replace and rich-text body create/replace gates, note delete outside the approved exact-note delete gate, folder delete outside the approved exact empty child-folder delete gate, move outside the approved exact-note/exact-folder move-to-folder or exact empty child-folder move gates, folder/account targeting outside exact note create, exact child-folder create, exact-folder rename, exact empty child-folder delete, exact empty child-folder move, and note move by `notes:folder:v1:` handle, root/recursive folder delete, root/non-empty/recursive/cross-account folder move, root/default-account folder creation, broad body search, checklist state, attachment mutation, broad attachment export, locked/shared-note mutation, Recently Deleted management, or bulk operations.
- Calendar update outside the approved exact-event title/time/location/structured-location/notes/all-day/relative-or-absolute display/audio/email/geofence alarm/time-zone/availability/event-URL set-or-clear/first-visible-or-mid-series-recurrence-clear-or-replace gate, delete outside the approved exact-event absence-proof gate, recurrence outside simple count-, end-date-, or explicit-unbounded daily/weekly/monthly/yearly create, add-to-non-recurring-event update, weekly weekday selection, monthly weekday selection, monthly day-of-month selection, monthly nth-weekday selection, yearly month/month-day/month-nth-weekday/day-of-year/week-of-year plus explicit weekday selection for week-of-year and selector-backed set-position filtering, selected-occurrence title/plain-location/notes/timed reschedule/availability/event URL set/clear/structured-location set/clear/display-alarm set/clear/action-alarm set/clear/all-day set/clear/date-only reschedule/target-calendar move update, selected-occurrence delete, future-event recurring span delete, whole-series recurring-event delete, first-visible or mid-series recurrence clearing, mid-series recurrence replacement, or future-series recurring-event title/plain-location/notes/timed reschedule/availability/event URL set/clear/structured-location set/clear/display-alarm set/clear/action-alarm set/clear/all-day set/clear/date-only reschedule/target-calendar move update, or non-synthetic calendar create/rename/delete, default-calendar mutation, calendar delete outside the synthetic event-only bounded-empty gate, source/account management, custom recurrence shapes beyond approved selector-backed EventKit rules, implicit unbounded recurrence, attendees, invitations, non-allow-listed event URL schemes, attachments, travel time, availability outside explicit busy/free/tentative/unavailable support-mask validation, silent default-calendar guessing or default-calendar mutation, timed-event time-zone inference, procedure alarms, or bulk operations.
- Contacts raw identifier input, broad contact dumps in chat, direct database writes, duplicate merge automation, free-form labels over 255 characters or containing control characters, delete outside the approved exact-contact delete gate, group CRUD outside the approved exact group/container gate, and complete Contacts management outside the approved live create/update/group/group-crud/batch/delete gates. Exact note contracts are synthetic-testable but live-unavailable and fail closed with `contacts_note_unavailable` before mutation.
- Reminders bulk mutation, list/account management outside exact same-source list-move and exact list create/rename/empty-delete/same-source migrate-delete, cross-account/source list move, cross-source migrate-delete, attachment mutation, URL mutation outside the approved exact URL update/clear gate, alarm mutation outside the approved exact absolute/relative/mixed display-alarm set/clear/start-date set/clear/recurrence create/update/clear gate, raw alarm-state return, mixed-with-audio/email/geofence/procedure Reminder alarm mutation, rich-content mutation, delete outside the approved exact-handle delete gate, list move outside the approved exact expected-current-list plus target-list identity-proof gate, or title/notes/priority/URL mutation outside the approved exact-handle update gates.
- iCloud Drive folder creation outside the exact create-folder or bounded create-folder-path gates, folder Trash outside the exact folder trash gate, folder delete outside the exact selected-folder delete gate, folder move outside the exact folder move gate, folder copy outside the exact selected-folder copy gate, trash/delete outside the exact trash-text, exact folder trash, exact regular-file trash, or exact delete-file gates, file permanent delete outside the exact delete-text or delete-file gates, import outside the exact import-file gate, rename/copy/move outside the exact text-file or regular-file gates, empty Trash, binary/document content generation, regular-file mutation outside exact import-file, exact replace-file, exact trash-file, exact delete-file, or metadata-only rename/copy/move gates, raw path writes, hidden-file writes, symlink/package traversal, recursive folder writes/deletes, unbounded folder copy, broad folder copy, or content replacement outside the approved exact-handle replace-text or replace-file gates.
- Bulk mutation outside explicitly approved exact-handle batch gates.
- Mutation through iCloud.com, browser sessions, keychain credentials, private iCloud web APIs, OAuth, IMAP, or connector fallbacks.

## Verification Shape

A mutation tranche is not done until:

- The read-only tools still pass all existing tests.
- New preview/apply/read-back tests pass.
- `uv run python scripts/audit_mutation_gates.py` is updated for the approved tool names and passes.
- `uv run python scripts/audit_write_design_gates.py` is updated from design-only to approved-with-tests for the exact operation and passes.
- Runtime smoke proves the tool list annotations and refusal behavior.
- The skill, privacy model, threat model, testing doc, capability matrix, README, and plugin manifest all describe the new mutation state consistently.
- Installed cache and cross-agent sync verification pass.
