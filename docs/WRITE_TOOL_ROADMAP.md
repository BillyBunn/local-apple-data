# Write Tool Roadmap

Current-install note: the MCP server exposes 14 approved apply tools. The Contacts note append/set/clear/merge entries in the design inventory below describe synthetic-tested sub-gates inside `contacts_apply_change`, not a live-usable operation: the current locally signed helper lacks Apple's restricted notes entitlement and fails closed with `contacts_note_unavailable`. Other approved Contacts operations remain available.

The current release is read-mostly. Safari bookmarks/Reading List/folder metadata, Shortcuts metadata/selected-folder shortcut metadata, Books metadata/selected-book annotations, Podcasts metadata/selected-episode descriptions, Music track/playlist metadata, TV item/playlist/selected-playlist item metadata, and Freeform board/folder/selected-folder board/child-folder metadata are read-only. Reminders create/complete/uncomplete/due-date/title/notes/priority-update/exact URL update/clear/exact absolute/relative/mixed display-alarm set/clear/start-date set/clear/recurrence create/update/clear/exact same-source list-move/delete apply and exact list create/rename/empty-delete/same-source migrate-delete apply, iCloud Drive create-folder/create-folder-path/rename-folder/trash-folder/delete-folder/move-folder/copy-folder/create/append-text/replace-text/trash-text/delete-text/rename-text/copy-text/move-text/rename-file/copy-file/move-file/import-file/replace-file/trash-file/delete-file apply, Calendar create-event/update/delete apply including exact relative alarm offsets, exact absolute display alarms, exact audio alarms, exact email alarms, exact structured geofence alarms, explicit timed-event time zones, exact availability create/update with expected-state binding, exact allow-listed event URL create/update and exact event URL clearing with hash-only read-back proof, exact target-calendar create/move, date-only all-day inference, simple count-, end-date-, or explicit-unbounded daily/weekly/monthly/yearly recurrence create and add-to-non-recurring-event update with weekly weekday, monthly weekday, monthly day-of-month, monthly nth-weekday, and yearly month/month-day/month-nth-weekday/day-of-year/week-of-year plus explicit weekday selection for week-of-year and selector-backed set-position filtering, selected-occurrence recurring-event title/plain-location/notes/timed reschedule/availability/event URL set/clear/structured-location set/clear/display-alarm set/clear/action-alarm set/clear/all-day set/clear/date-only reschedule/target-calendar move update, selected-occurrence recurring-event delete, future-event recurring span delete, whole-series recurring-event delete, first-visible and mid-series recurrence clearing, mid-series recurrence replacement, future-series recurring-event title/plain-location/notes/timed reschedule/availability/event URL set/clear/structured-location set/clear/display-alarm set/clear/action-alarm set/clear/all-day set/clear/date-only reschedule/target-calendar move update, and structured event location create/update/clear, apply from approved explicit default-calendar create plans only through the resolved exact calendar handle, and Calendar synthetic `LAD-TEST-*` calendar create/rename/delete apply, Contacts create-contact/exact scalar/method/rich-field/image update/exact group membership/exact group create/rename/delete/exact batch/delete apply, Notes default/exact-folder note create, exact child-folder create, exact-folder rename, exact empty child-folder delete, exact empty child-folder move, append-text, replace-text, rich-text body create, rich-text body replace, move-to-folder, and exact-note delete apply, Mail create-draft/send-message/reply-message/reply-all-message/forward-message/mark-read/mark-unread/flag-message/unflag-message/archive-message/move-message/trash-message apply including capped exact bulk triage, Mail synthetic `LAD-TEST-*` mailbox create/rename apply, source-gated synthetic mailbox delete and synthetic cleanup apply only when public Mail.app deletion plus exact target-state binding, mailbox-scoped absence proof, and Mail-idle guards succeed, Photos import, exact asset favorite/hidden update, exact asset delete, exact regular-album membership add/remove, and exact regular-album create/rename/delete apply with full Photos Library authorization, and Messages send-text/send-file apply, home-directory Filesystem create-folder/create-folder-path/rename-folder/trash-folder/delete-folder/move-folder/copy-folder/create/append-text/replace-text/trash-text/delete-text/rename-text/copy-text/move-text/rename-file/copy-file/move-file/import-file/replace-file/trash-file/delete-file apply rooted at the operator home directory reusing the exact iCloud Drive within-root plan/apply/read-back gates with a credential/secret path denylist, and Shortcuts run apply of one exact identifier-bound shortcut through the plan/apply approval-token/confirm gate, resolved by an exact `shortcuts:item:v1:` handle with the resolved identifier bound into the approval fingerprint, invoked by argv (never a shell string) under a hard execution timeout, proving invocation of the named shortcut only because a shortcut's arbitrary side effects are not verifiable by read-back are the only approved write surfaces. Every other write surface remains gated by this roadmap and `docs/MUTATION_GATES.md`.

Roadmap priority as of 2026-07-03 was Calendar agent-ready scheduling first, then Files/iCloud Drive broadening, safe Mail gaps only while destructive cleanup is Mail.app-health blocked, Messages blocker/implementation, and Contacts polish. That ordering has been overtaken by events -- v1.178 through v1.184 landed in Filesystem, Notes, Reminders, and Mail. There is no current agreed next priority; picking one is an operator decision. Treat `CHANGELOG.md` as the authority on where the work actually is.

iCloud Drive exact regular-file export is governed by `docs/V1_85_ICLOUD_DRIVE_FILE_EXPORT.md`. It is a read-only exact-handle export gate, not a mutation gate or approval for binary/document content generation, regular-file mutation outside exact import-file, exact replace-file, exact trash-file, exact delete-file, or metadata-only rename/copy/move gates.

Use this file with `docs/MUTATION_GATES.md`. The full CRUD priority kickoff gate is `docs/V1_33_FULL_CRUD_PRIORITY_PLAN.md`; it makes Mail, Messages, Calendar, Reminders, and Notes CRUD expansion a first-class priority but does not approve any new mutation by itself. Contacts rich update, note set/clear/merge, group membership, and exact batch are gated by `docs/V1_71_CONTACTS_RICH_UPDATE_GROUP_BATCH_DESIGN.md`; Contacts exact group create/rename/delete and exact container-targeted create are gated by `docs/V1_72_CONTACTS_GROUP_CRUD_WRITE_DESIGN.md`. The first concrete Reminders write design gate is `docs/V1_11_REMINDERS_WRITE_DESIGN.md`; the destructive exact-handle Reminders delete gate is `docs/V1_35_REMINDERS_DELETE_WRITE_DESIGN.md`; the Reminders exact same-source list-move gate is `docs/V1_65_REMINDERS_LIST_MOVE_WRITE_DESIGN.md`; the Reminders exact URL update/clear gate is `docs/V1_136_REMINDERS_URL_WRITE_DESIGN.md`; the Reminders exact absolute display-alarm set/clear gate is `docs/V1_137_REMINDERS_ABSOLUTE_DISPLAY_ALARM_WRITE_DESIGN.md`; the Reminders exact relative display-alarm set and broadened pure display-alarm clear gate is `docs/V1_138_REMINDERS_RELATIVE_DISPLAY_ALARM_WRITE_DESIGN.md`; the Reminders exact mixed absolute-plus-relative display-alarm set/clear gate is `docs/V1_176_REMINDERS_MIXED_DISPLAY_ALARM_WRITE_DESIGN.md`; the Reminders exact start-date set/clear gate is `docs/V1_177_REMINDERS_START_DATE_WRITE_DESIGN.md`; the Reminders exact recurrence create/update/clear gate is `docs/V1_177_REMINDERS_RECURRENCE_WRITE_DESIGN.md`; the Reminders exact list create/rename/empty-delete gate is `docs/V1_155_REMINDERS_LIST_CRUD_WRITE_DESIGN.md`; the Reminders same-source list migration delete gate is `docs/V1_156_REMINDERS_LIST_MIGRATION_DELETE_WRITE_DESIGN.md`; the first iCloud Drive create-text write design gate is `docs/V1_12_ICLOUD_DRIVE_WRITE_DESIGN.md`; the first iCloud Drive append-text write design gate is `docs/V1_18_ICLOUD_DRIVE_APPEND_WRITE_DESIGN.md`; the iCloud Drive replace-text write design gate is `docs/V1_51_ICLOUD_DRIVE_REPLACE_WRITE_DESIGN.md`; the iCloud Drive create-folder write design gate is `docs/V1_52_ICLOUD_DRIVE_FOLDER_CREATE_WRITE_DESIGN.md`; the iCloud Drive trash-text write design gate is `docs/V1_53_ICLOUD_DRIVE_TRASH_WRITE_DESIGN.md`; the iCloud Drive rename/copy/move text-file write design gate is `docs/V1_54_ICLOUD_DRIVE_RENAME_COPY_MOVE_WRITE_DESIGN.md`; the iCloud Drive exact folder rename gate is `docs/V1_60_ICLOUD_DRIVE_FOLDER_RENAME_WRITE_DESIGN.md`; the iCloud Drive exact folder Trash gate is `docs/V1_61_ICLOUD_DRIVE_FOLDER_TRASH_WRITE_DESIGN.md`; the iCloud Drive exact folder move gate is `docs/V1_62_ICLOUD_DRIVE_FOLDER_MOVE_WRITE_DESIGN.md`; the iCloud Drive exact selected-folder copy gate is `docs/V1_63_ICLOUD_DRIVE_FOLDER_COPY_WRITE_DESIGN.md` plus `docs/V1_147_ICLOUD_DRIVE_NON_EMPTY_FOLDER_COPY_WRITE_DESIGN.md`; the iCloud Drive exact selected-folder permanent delete gate is `docs/V1_67_ICLOUD_DRIVE_FOLDER_DELETE_WRITE_DESIGN.md`; the iCloud Drive exact text-file permanent delete gate is `docs/V1_68_ICLOUD_DRIVE_DELETE_TEXT_WRITE_DESIGN.md`; the first Calendar write design gate is `docs/V1_13_CALENDAR_WRITE_DESIGN.md`; the Calendar exact-event update gate is `docs/V1_34_CALENDAR_UPDATE_WRITE_DESIGN.md`; the Calendar exact-event delete gate is `docs/V1_36_CALENDAR_DELETE_WRITE_DESIGN.md`; the Calendar explicit all-day gate is `docs/V1_55_CALENDAR_ALL_DAY_WRITE_DESIGN.md`; the Calendar exact alarm-offset gate is `docs/V1_56_CALENDAR_ALARM_WRITE_DESIGN.md`; the Calendar exact target-calendar selection/move gate is `docs/V1_86_CALENDAR_TARGET_CALENDAR_MOVE_WRITE_DESIGN.md`; the Calendar explicit timed-event time-zone gate is `docs/V1_87_CALENDAR_TIME_ZONE_WRITE_DESIGN.md`; the Calendar exact absolute display-alarm gate is `docs/V1_88_CALENDAR_ABSOLUTE_ALARM_WRITE_DESIGN.md`; the Calendar simple recurrence create gate is `docs/V1_89_CALENDAR_RECURRENCE_WRITE_DESIGN.md`; the Calendar monthly/yearly recurrence and date-only all-day inference gate is `docs/V1_90_CALENDAR_RECURRENCE_DATE_ONLY_WRITE_DESIGN.md`; the Calendar explicit default-calendar create planning gate is `docs/V1_91_CALENDAR_DEFAULT_CALENDAR_CREATE_WRITE_DESIGN.md`; the Calendar exact availability gate is `docs/V1_92_CALENDAR_AVAILABILITY_WRITE_DESIGN.md`; the Calendar recurrence update gate is `docs/V1_93_CALENDAR_RECURRENCE_UPDATE_WRITE_DESIGN.md`; the Calendar exact allow-listed event URL gate is `docs/V1_94_CALENDAR_EVENT_URL_WRITE_DESIGN.md`; the Calendar exact event URL clearing gate is `docs/V1_95_CALENDAR_EVENT_URL_CLEAR_WRITE_DESIGN.md`; the Calendar selected recurring occurrence delete gate is `docs/V1_96_CALENDAR_RECURRING_OCCURRENCE_DELETE_WRITE_DESIGN.md`; the Calendar future-event recurring span delete gate is `docs/V1_97_CALENDAR_RECURRING_FUTURE_DELETE_WRITE_DESIGN.md`; the first Contacts write design gate is `docs/V1_14_CONTACTS_WRITE_DESIGN.md`; the Contacts exact scalar update gate is `docs/V1_48_CONTACTS_UPDATE_WRITE_DESIGN.md`; the Contacts exact email/phone/URL method-array replacement gate is `docs/V1_69_CONTACTS_METHOD_UPDATE_WRITE_DESIGN.md`; the Contacts backup and exact note append gate is `docs/V1_70_CONTACTS_BACKUP_AND_NOTE_APPEND_DESIGN.md`; the Contacts exact delete gate is `docs/V1_49_CONTACTS_DELETE_WRITE_DESIGN.md`; the first Notes write design gate is `docs/V1_15_NOTES_WRITE_DESIGN.md`; the Notes exact-folder note-create gate is `docs/V1_39_NOTES_FOLDER_CREATE_WRITE_DESIGN.md`; the Notes exact child-folder create gate is `docs/V1_57_NOTES_FOLDER_CREATE_WRITE_DESIGN.md`; the Notes exact folder rename gate is `docs/V1_58_NOTES_FOLDER_RENAME_WRITE_DESIGN.md`; the Notes exact empty child-folder delete gate is `docs/V1_59_NOTES_FOLDER_DELETE_WRITE_DESIGN.md`; the Notes exact-note move-to-folder gate is `docs/V1_45_NOTES_MOVE_WRITE_DESIGN.md`; the Notes exact-delete gate is `docs/V1_42_NOTES_DELETE_WRITE_DESIGN.md`; the first Mail draft write design gate is `docs/V1_16_MAIL_DRAFT_WRITE_DESIGN.md`; the Mail create-draft exact sender gate is `docs/V1_74_MAIL_SENDER_DRAFT_WRITE_DESIGN.md`; the Mail outbound exact sender gate is `docs/V1_82_MAIL_OUTBOUND_SENDER_SELECTION_WRITE_DESIGN.md`; the Mail create-draft local file attachment gate is `docs/V1_75_MAIL_DRAFT_ATTACHMENT_WRITE_DESIGN.md`; the Mail outbound local file attachment gate is `docs/V1_76_MAIL_OUTBOUND_ATTACHMENT_WRITE_DESIGN.md`; the Mail bounded search discovery read gate is `docs/V1_77_MAIL_SEARCH_DISCOVERY_DESIGN.md`; the Mail attachment content/PDF/OCR snippet gate is `docs/V1_79_MAIL_ATTACHMENT_CONTENT_SEARCH_DESIGN.md`; the Mail opt-in private FTS index gate is `docs/V1_81_MAIL_FTS_INDEX_DESIGN.md`; the Mail signature/template/query-result triage gate is `docs/V1_83_MAIL_SIGNATURE_TEMPLATE_QUERY_TRIAGE_DESIGN.md`; the Mail synthetic mailbox/cleanup gate is `docs/V1_84_MAIL_SYNTHETIC_MAILBOX_CLEANUP_WRITE_DESIGN.md`; the Mail send-message write design gate is `docs/V1_43_MAIL_SEND_WRITE_DESIGN.md`; the Mail reply-message write design gate is `docs/V1_44_MAIL_REPLY_WRITE_DESIGN.md`; the Mail reply-all-message write design gate is `docs/V1_73_MAIL_REPLY_ALL_WRITE_DESIGN.md`; the Mail forward-message write design gate is `docs/V1_50_MAIL_FORWARD_WRITE_DESIGN.md`; the Mail exact-message read-state gate is `docs/V1_32_MAIL_TRIAGE_WRITE_DESIGN.md`; the Mail exact-message flag/unflag gate is `docs/V1_37_MAIL_FLAG_WRITE_DESIGN.md`; the Mail exact-message archive gate is `docs/V1_40_MAIL_ARCHIVE_WRITE_DESIGN.md`; the Mail exact-message Trash gate is `docs/V1_41_MAIL_TRASH_WRITE_DESIGN.md`; the Mail exact-message target-mailbox move gate is `docs/V1_46_MAIL_MOVE_WRITE_DESIGN.md`; the Mail exact-message cross-account target-mailbox move gate is `docs/V1_66_MAIL_CROSS_ACCOUNT_MOVE_WRITE_DESIGN.md`; the first Photos import write design gate is `docs/V1_17_PHOTOS_IMPORT_WRITE_DESIGN.md`; the first Notes append-text write design gate is `docs/V1_19_NOTES_APPEND_WRITE_DESIGN.md`; the Notes replace-text write design gate is `docs/V1_34_NOTES_REPLACE_WRITE_DESIGN.md`; the first Notes attachment export design gate is `docs/V1_20_NOTES_ATTACHMENT_EXPORT.md`; the first Mail attachment export design gate is `docs/V1_21_MAIL_ATTACHMENT_EXPORT.md`; the first Messages attachment export design gate is `docs/V1_22_MESSAGES_ATTACHMENT_EXPORT.md`; the Messages attributed-body fallback design gate is `docs/V1_23_MESSAGES_ATTRIBUTED_BODY.md`; the Messages participant metadata read gate is `docs/V1_64_MESSAGES_PARTICIPANTS_METADATA.md`; the first Messages send-text write design gate is `docs/V1_24_MESSAGES_SEND_TEXT_WRITE_DESIGN.md`; the first Messages send-file write design gate is `docs/V1_38_MESSAGES_SEND_FILE_WRITE_DESIGN.md`; the Messages risky-mutation source-review blocker gate is `docs/V1_47_MESSAGES_RISKY_MUTATION_SOURCE_REVIEW.md`; the Safari read design gate is `docs/V1_25_SAFARI_BOOKMARKS.md`; the Shortcuts metadata design gate is `docs/V1_26_SHORTCUTS_METADATA.md`; the Books metadata/selected annotations design gate is `docs/V1_27_BOOKS_METADATA.md`; the Podcasts metadata design gate is `docs/V1_28_PODCASTS_METADATA.md`; the Music metadata design gate is `docs/V1_29_MUSIC_METADATA.md`; the TV metadata design gate is `docs/V1_30_TV_METADATA.md`; the Freeform metadata design gate is `docs/V1_31_FREEFORM_METADATA.md`; real non-synthetic Mail permanent delete remains gated.

The Calendar whole-series recurring-event delete gate is `docs/V1_98_CALENDAR_RECURRING_SERIES_DELETE_WRITE_DESIGN.md`. The Calendar weekly weekday recurrence gate is `docs/V1_99_CALENDAR_WEEKLY_WEEKDAY_RECURRENCE_WRITE_DESIGN.md`. The Calendar first-visible and mid-series recurrence clear gate is `docs/V1_100_CALENDAR_RECURRENCE_CLEAR_WRITE_DESIGN.md`. The Calendar mid-series recurrence replacement gate is `docs/V1_126_CALENDAR_RECURRENCE_REPLACEMENT_WRITE_DESIGN.md`. The Calendar monthly day-of-month recurrence gate is `docs/V1_101_CALENDAR_MONTHDAY_RECURRENCE_WRITE_DESIGN.md`. The Calendar structured event location create/update gate is `docs/V1_102_CALENDAR_STRUCTURED_LOCATION_WRITE_DESIGN.md`. The Calendar exact audio alarm gate is `docs/V1_103_CALENDAR_AUDIO_ALARM_WRITE_DESIGN.md`. The Calendar structured geofence alarm gate is `docs/V1_104_CALENDAR_GEOFENCE_ALARM_WRITE_DESIGN.md`. The Calendar monthly nth-weekday recurrence gate is `docs/V1_105_CALENDAR_MONTHLY_NTH_WEEKDAY_RECURRENCE_WRITE_DESIGN.md`. The Calendar yearly month recurrence gate is `docs/V1_106_CALENDAR_YEARLY_MONTH_RECURRENCE_WRITE_DESIGN.md`. The Calendar structured event location clearing gate is `docs/V1_107_CALENDAR_STRUCTURED_LOCATION_CLEAR_WRITE_DESIGN.md`. The Calendar exact email alarm gate is `docs/V1_108_CALENDAR_EMAIL_ALARM_WRITE_DESIGN.md`. The Calendar recurrence end-date gate is `docs/V1_109_CALENDAR_RECURRENCE_END_DATE_WRITE_DESIGN.md`. The Calendar yearly day/week recurrence gate is `docs/V1_110_CALENDAR_YEARLY_DAY_WEEK_RECURRENCE_WRITE_DESIGN.md`. The Calendar yearly month nth-weekday recurrence gate is `docs/V1_111_CALENDAR_YEARLY_MONTH_NTH_WEEKDAY_RECURRENCE_WRITE_DESIGN.md`. The Calendar yearly month day-of-month recurrence gate is `docs/V1_112_CALENDAR_YEARLY_MONTH_DAY_RECURRENCE_WRITE_DESIGN.md`. The Calendar monthly weekday recurrence gate is `docs/V1_113_CALENDAR_MONTHLY_WEEKDAY_RECURRENCE_WRITE_DESIGN.md`. The Calendar selected recurring occurrence scalar update gate is `docs/V1_114_CALENDAR_SELECTED_OCCURRENCE_UPDATE_WRITE_DESIGN.md`.

Mail capped exact bulk triage is governed by `docs/V1_78_MAIL_BULK_TRIAGE_WRITE_DESIGN.md`.

Current progress: `reminders plan` / `reminders_plan_change`, `icloud-drive plan` / `icloud_drive_plan_change`, `calendar plan` / `calendar_plan_change`, `calendar plan-calendar` / `calendar_plan_calendar_change`, `contacts plan` / `contacts_plan_change`, `notes plan` / `notes_plan_change`, `mail plan` / `mail_plan_change`, `photos plan` / `photos_plan_change`, and `messages plan` / `messages_plan_change` implement non-mutating previews. `reminders lists` / `reminders_search_lists` and `reminders list` / `reminders_get_list` implement exact Reminders target-list metadata selection by opaque `reminders:list:eventkit:v1:` handle. `reminders apply` / `reminders_apply_change` implement approved Reminder create, complete, uncomplete, due-date update, title update, notes update with current-notes-SHA drift refusal, priority update with expected-current-priority checks, exact URL update/clear with expected URL-state binding, allowed URL schemes, EventKit apply, hash-only URL read-back or absence proof, exact same-source list-move with exact reminder handle, exact expected current-list handle, exact same-source target-list handle, expected title/completed/current-list binding, EventKit reassignment, and `target_list_verified:true` identity proof, and exact-handle delete with expected completed state, priority, current-notes-SHA drift refusal, EventKit removal, and read-back absence proof. `reminders apply-list` / `reminders_apply_list_change` implement exact list create/rename/empty-delete/same-source migrate-delete with exact source/target handles, duplicate-title refusal, writable/source-state binding, empty-list proof for rename/delete, EventKit save/remove apply, and source/title/absence read-back proof. `icloud-drive apply` / `icloud_drive_apply_change` implement approved iCloud Drive create-folder with exact parent handles, no-follow parent validation, exclusive mkdir, metadata-only read-back, and existing-directory idempotency; approved exact folder rename with exact directory handles, metadata SHA-256 binding, no-overwrite fd-relative rename, metadata-only read-back, non-empty child preservation, `non_empty_allowed:true`, and apply-time read-back mismatch partial reporting; approved exact folder Trash with exact directory handles, metadata SHA-256 binding, recoverable Trash move, metadata-only original absence proof, non-empty child preservation, `non_empty_allowed:true`, and cleanup/read-back partial reporting; approved exact selected-folder permanent delete with exact directory handles, metadata SHA-256 binding, private bounded source-tree binding, hidden/symlink/package/tree-size refusal, hidden staging identity proof, bounded permanent staged-tree removal, metadata-only original absence proof, `non_empty_allowed:true`, no child listing/content/hash/path return, and race rollback/partial reporting that never claims permanent deletion on partial verification failure; approved exact folder move with exact directory and target-parent handles, metadata SHA-256 binding, descendant-parent refusal, no-overwrite fd-relative move, metadata-only source/target presence proof, non-empty child preservation, `non_empty_allowed:true`, and apply-time read-back mismatch partial reporting; approved exact selected-folder copy with exact directory and target-parent handles, metadata SHA-256 binding, private bounded source-tree binding, hidden/symlink/package/tree-size refusal, descendant-parent refusal, no-overwrite recursive copy, metadata-only source-preservation and target-presence proof, `non_empty_allowed:true`, no child listing, and race cleanup/partial reporting; approved create-text with approval-token checks, exclusive create, and read-back verification; approved append-text with exact file handles, expected-current-SHA-256 drift refusal, and read-back hash verification; approved replace-text with exact file handles, expected-current-SHA-256 drift refusal, strict bounded UTF-8 replacement, no-follow reads, package/symlink traversal refusal, same-directory temp replacement with immediate pre-replace SHA recheck, and read-back hash verification; approved trash-text with exact file handles, expected-current-SHA-256 drift refusal, supported text-file validation, recoverable Trash move, no permanent unlink, no raw Trash path return, and original-handle absence proof; approved delete-text with exact file handles, expected-current-SHA-256 drift refusal, exact file identity approval binding, stale-token replay refusal for recreated same-path/same-content files, random-only hidden staging identity proof, permanent unlink, and original absence proof without returning content/hash/path data; plus approved rename-text/copy-text/move-text with exact file handles, expected-current-SHA-256 drift refusal, no-overwrite targets, fd-relative no-follow local filesystem operations, source/target presence proof, and target hash read-back without content text. `calendar calendars` / `calendar_search_calendars` and `calendar calendar` / `calendar_get_calendar` implement exact Calendar target metadata selection by opaque `calendar:calendar:v1:` handle. `calendar apply` / `calendar_apply_change` implement approved Calendar timed/date-only-all-day/relative-or-absolute display/audio/email/geofence alarm/availability/exact allow-listed event URL/simple count-, end-date-, or explicit-unbounded daily/weekly/monthly/yearly recurrence create-event and add-to-non-recurring-event update with approval-token checks, explicit calendar title, exact target calendar handle, or plan-only default-calendar resolution to an exact handle, explicit all-day/date-only/relative-or-absolute display/audio/email/geofence alarm/availability/event URL/recurrence binding, EventKit support-mask validation for availability, allow-listed event URL validation, EventKit apply, and read-back verification including hash-only URL proof; exact-event title/time/location/structured-location/notes/all-day/relative-or-absolute display/audio/email/geofence alarm/availability/event URL update or URL clearing plus optional exact target-calendar move and optional simple recurrence add-to-non-recurring-event update with expected current-state binding and unsupported-event refusal; plus exact-event delete with expected-state binding including all-day, relative-or-absolute alarm, optional availability state, optional event URL presence/hash state, and selected-occurrence recurring-event title/plain-location/notes/timed reschedule/availability/event URL set/clear/structured-location set/clear/display-alarm set/clear/action-alarm set/clear/all-day set/clear/date-only reschedule/target-calendar move update through `recurrence_update_scope:this_event` with selected/adjacent occurrence proof, selected-occurrence recurring-event delete through `recurrence_delete_scope:this_event`, optional future-event recurring span delete through `recurrence_delete_scope:future_events`, and optional whole-series recurring-event delete through `recurrence_delete_scope:all_events`, optional update-only first-visible and mid-series recurrence clearing through `clear_recurrence`, optional update-only mid-series recurrence replacement through `recurrence_update_scope:future_events` plus approved replacement recurrence fields, unsupported-event refusal, EventKit removal, destructive MCP annotation, selected-occurrence/future-span/whole-series absence proof, first-visible and mid-series recurrence clear read-back/absence proof, adjacent- or previous-occurrence preservation/absence proof, or read-back absence proof. `calendar apply-calendar` / `calendar_apply_calendar_change` implement approved synthetic `LAD-TEST-*` calendar create/rename/delete with exact source or target `calendar:calendar:v1:` handles, safe-hash binding, same-source duplicate-title refusal, bounded empty-calendar safety-window proof for rename/delete, event-only delete refusal, public EventKit `saveCalendar` apply for create/rename, public EventKit `removeCalendar` apply for delete, source/title read-back for create, title read-back for rename, and absence proof for delete. silent default-calendar guessing/mutation, real/non-synthetic calendar management, calendar delete outside the synthetic event-only bounded-empty gate, source/account management, availability outside busy/free/tentative/unavailable support-mask validation, non-allow-listed event URL schemes, and recurring existing-event mutation outside approved selected-update/add/clear/replacement/delete paths remain blocked. `contacts count` / `contacts_count` and `contacts export` / `contacts_export_archive` implement read-only Contacts count and JSON plus vCard backup export with count-match verification. `contacts groups` / `contacts_search_groups` and `contacts group` / `contacts_get_group` implement read-only Contacts group metadata selection by opaque `contacts:group:v1:` handle. `contacts containers` / `contacts_search_containers` and `contacts container` / `contacts_get_container` implement read-only Contacts container metadata selection by opaque `contacts:container:v1:` handle. `contacts apply` / `contacts_apply_change` implement approved Contacts create-contact with optional exact container targeting, approval-token checks, Contacts.framework apply, and read-back verification; exact-contact scalar/method/rich-field/image update with `contacts:contact:v1:` handle, `update_safe_sha256` drift refusal, omitted-array preservation, provided-array replacement, explicit empty-array clears, Contacts.framework apply, and read-back verification; exact note append/set/clear/merge with `note_safe_sha256` drift refusal and hash-only note read-back; exact group membership with contact/group safe-hash binding and metadata-only group read-back; exact group create/rename/delete with group/container safe-hash binding and metadata-only read-back or absence proof with `contacts_deleted:false`; capped exact batch over approved existing-contact operations; plus exact-contact delete with `delete_safe_sha256` drift refusal, Contacts.framework deletion, destructive MCP annotation, and absence read-back proof. `notes folders` / `notes_search_folders` and `notes folder` / `notes_get_folder` implement exact Notes folder metadata selection by opaque `notes:folder:v1:` handle. `notes apply` / `notes_apply_change` implement approved Notes create-note with approval-token checks, optional exact folder handle targeting, Notes.app automation, and exact-content plus folder read-back verification, approved exact child-folder create with exact normal parent folder handle targeting, same-parent idempotency, Notes.app `make new folder at targetFolder` automation, metadata-only read-back, and selected-parent proof, approved exact-folder rename with exact normal folder handle targeting, expected folder-title SHA-256 binding, Notes.app `set name of targetFolder` automation, already-applied retry handling, and metadata-only read-back, approved exact empty child-folder delete with exact normal child folder handle targeting, expected folder-title SHA-256 binding, Notes.app `delete targetFolder` automation, and absence read-back, approved append-text with exact note handles, expected-current-SHA-256 drift refusal, shared/locked-note refusal, and exact-content read-back hash verification, approved replace-text with exact note handles, expected-current-SHA-256 drift refusal, locked/deleted/shared-note refusal, and exact-content read-back verification, approved move-to-folder with exact note and folder handles, expected-current-SHA-256 drift refusal, same-account target-folder proof, locked/deleted/shared-note refusal, Notes.app move automation, and folder read-back verification with `body_returned:false`, plus approved exact-note delete with expected-current-SHA-256 drift refusal, locked/deleted/shared-note refusal, destructive MCP annotation, Notes.app delete, and absence read-back verification. `notes attachments` / `notes_list_attachments` and `notes export-attachment` / `notes_export_attachment` implement exact-handle Notes attachment metadata/export without inline bytes or mutation. `mail attachments` / `mail_list_attachments` and `mail export-attachment` / `mail_export_attachment` implement exact-message-plus-attachment-handle Mail MIME attachment metadata/export without inline bytes, source message paths, externalized fetch, broad export, or mutation. `mail mailboxes` / `mail_search_mailboxes` and `mail mailbox` / `mail_get_mailbox` implement exact Mail target-mailbox metadata selection by opaque `mail:mailbox:v1:` handle. `mail apply` / `mail_apply_change` implement approved Mail create-draft with approval-token checks, optional exact sender selection, optional bounded caller-selected local file attachments, save-only Mail.app automation, and local Drafts read-back verification when available, approved irreversible send-message with approval-token checks, scoped Mail.app send automation, local Sent-copy read-back when available, destructive MCP annotation, and no sent body echo, approved exact-message sender-only reply-message with source-state drift refusal, scoped Mail.app reply automation, local Sent-copy read-back when available, no direct-recipient override, and no reply body echo, approved exact-message reply-all-message with source-state drift refusal, scoped Mail.app reply-all automation, local Sent-copy read-back when available, no direct-recipient override, and no reply body echo, approved exact-message forward-message with explicit recipients, source-state and source attachment/non-body-part state drift refusal, scoped Mail.app forward automation, local Sent-copy read-back when available, no source-content/body echo, optional source attachment-like part preservation through explicit `include_source_attachments`, pre-send Mail attachment-count verification, and no source attachment paths, bytes, source body, raw MIME, or per-part Sent proof, plus approved exact-message and capped exact bulk mark-read/mark-unread/flag-message/unflag-message/archive-message/move-message/trash-message with `.emlx` RFC Message-ID bridging, stale read/flag/mailbox-state refusal, same-account Archive/Trash resolution, exact target-mailbox resolution including cross-account exact targets, capped bulk preflight/read-back/partial reporting, destructive MCP annotation because the static tool can send external mail, attach caller-selected local files to saved drafts, and move messages to Trash, no permanent deletion, and local read-back verification. `photos apply` / `photos_apply_change` implement approved Photos import with approval-token checks, source-file hash binding, PhotoKit apply, and created-asset read-back verification; exact asset favorite/hidden update with expected-state binding and read-back; exact selected-asset delete with expected-state binding and absence proof; exact regular-album membership add/remove with exact asset/album handles, expected membership binding, and membership read-back; and exact regular-album create/rename/delete with full Photos Library authorization, bounded title validation, duplicate-title refusal, exact album-state binding, empty-delete proof, and title or absence read-back. `messages apply` / `messages_apply_change` implement approved Messages send-text and send-file to an exact existing chat with approval-token checks, stale chat/file-state refusal, Messages.app automation, ghost-row detection, and local `chat.db` read-back verification without returning the sent body, file bytes, or local file paths in apply output.

`mail senders` / `mail_search_senders` and `mail sender` / `mail_get_sender` implement masked exact configured sender metadata with opaque `mail:sender:v1:` handles. `mail body-search` / `mail_search_body`, `mail attachment-search` / `mail_search_attachments`, and `mail advanced-search` / `mail_search_advanced` implement date-bounded Mail discovery over local `.emlx` bodies, masked headers, attachment filename/MIME metadata, and opt-in attachment text/PDF/OCR snippets with capped snippets/results/cursor pagination and no inline attachment bytes, raw paths, raw IDs, or full body dumps. `mail fts-build` / `mail_build_fts_index` and `mail fts-search` / `mail_search_fts` implement an opt-in private local FTS cache for date-bounded audit discovery; build requires explicit confirmation, paginates with `next_cursor`, rejects unsafe cache, ancestor, and sidecar paths, rejects `reset` on continuation cursors, and reset validates then removes the private index plus WAL/SHM/journal sidecars before rebuilding from scratch. Search revalidates live rows, current date bounds, and content state, output returns capped redacted snippets plus exact handles only, and cache paths/raw cached text are never returned. `mail metadata` / `mail_get_metadata` now returns masked header fields and bounded attachment names when local `.emlx` metadata is available. `mail content` / `mail_get_content` supports `offset`/`next_offset` paging for exact selected handles. `mail apply` / `mail_apply_change` accepts `sender_handle` for `create_draft`, `send_message`, `reply_message`, `reply_all_message`, and `forward_message`; apply sets the public Mail.app outgoing-message sender before save/send, requires selected-sender read-back, and never returns the full sender email. `mail signatures`/`mail signature` expose signature metadata by opaque handle without returning signature bodies. `mail template-*` manages plugin-local templates whose bodies are explicitly user-provided. `mail plan-search-triage` converts capped FTS/search results into an exact bulk triage plan without query-result auto-apply. `mail plan-mailbox`/`mail apply-mailbox` manage only empty top-level `LAD-TEST-*` mailboxes, and `mail plan-cleanup`/`mail apply-cleanup` permanently delete or empty only synthetic `LAD-TEST-*` Trash/Junk messages after absence proof. Repeated exact `message_handles` are supported only for capped Mail bulk triage.

Trash-text's recoverable Trash move uses fd-based no-follow atomic swap into a reserved Trash target, post-swap SHA verification, swap-back on drift, removal only of the empty source reservation after proof, and original-handle absence read-back without returning a raw Trash path.

Delete-text's permanent unlink uses root-aware preview recomputation, exact file identity approval binding, stale-token replay refusal for recreated same-path/same-content files, fd-based no-follow random-only hidden staging, staged content/hash recheck, permanent unlink, and original-handle absence read-back without returning content, content hash, raw Trash path, staging path, or raw path.

`messages participants` / `messages_list_participants` and `messages participant` / `messages_get_participant` implement read-only exact selected-chat participant metadata through `messages:participant:v1:` handles. Participant list output returns service/count/timestamp metadata without phone/email previews; full participant identifiers are exact participant-handle detail only and remain blocked for mutation targeting.

## Principle

Write support must be boring, narrow, and independently verifiable. A write tool should never be the first way an agent discovers state. The agent must search or fetch current state first, present a preview, apply only after explicit approval, and then read back through an independent path.

## Tool Shape

Every mutation class should expose three layers:

- `preview`: validate inputs and return the planned change without touching Apple data. Planning tools are implemented for the approved apply surfaces, including `messages plan` / `messages_plan_change`.
- `apply`: perform the exact approved change. Apply tools are implemented for the approved apply surfaces, including `messages apply` / `messages_apply_change`.
- `read_back`: verify the resulting state through the normal read-only adapter.

The MCP tool annotations must mark write tools as not read-only. Destructive tools must be annotated as destructive and should stay absent until non-destructive writes are proven.

## First Write Tranche

Start with low-risk local writes through public Apple APIs or user-visible app automation:

| Priority | Surface | Operation | Preferred implementation | Read-back |
| --- | --- | --- | --- | --- |
| 1 | Reminders | Create reminder | Swift EventKit helper | Implemented; EventKit read-back |
| 2 | Reminders | Complete reminder | Swift EventKit helper | Implemented; EventKit read-back |
| 3 | Reminders | Complete, uncomplete, and update due date | Swift EventKit helper | Implemented; EventKit read-back |
| 4 | iCloud Drive | Create text file | Implemented; local filesystem with parent handle | Implemented; iCloud Drive metadata, content hash, and text content |
| 5 | Calendar | Create timed or explicit all-day event with exact alarm offsets and explicit timed-event IANA time zone | Implemented; Swift EventKit helper | Implemented; EventKit read-back |
| 6 | Contacts | Create contact | Implemented; Swift Contacts.framework helper | Implemented; Contacts.framework read-back |
| 7 | Notes | Create note | Implemented; Notes.app automation | Implemented; Notes metadata search and exact content |
| 8 | Mail | Create draft only | Implemented; save-only Mail.app automation | Implemented; Mail metadata search and exact content when local Drafts indexing is available |
| 9 | Photos | Import image/video asset; update exact asset favorite/hidden flags; delete one exact selected asset; exact regular-album membership add/remove; exact regular-album create/rename/delete with full Photos Library authorization | Implemented; Swift PhotoKit helper | Implemented; PhotoKit created-asset, exact asset flag read-back, exact asset absence proof, membership read-back, title read-back, or album absence proof |
| 10 | iCloud Drive | Append text to one exact text file | Implemented; local filesystem with exact file handle and expected current hash | Implemented; iCloud Drive metadata, new content hash, and text content |
| 11 | Notes | Append text to one exact note | Implemented; Notes.app automation with exact handle and expected current hash | Implemented; Notes exact content and new content hash |
| 12 | Notes | Attachment metadata/export for one exact note | Implemented read/export only; no mutation | Implemented; selected note handle plus selected attachment handle |
| 13 | Mail | Attachment metadata/export for one exact message | Implemented read/export only; no mutation | Implemented; selected message handle plus selected attachment handle |
| 14 | Messages | Send plaintext to one exact existing chat | Implemented; Messages.app AppleScript with exact chat handle | Implemented; local `chat.db` outgoing-row hash read-back and ghost-row detection |
| 15 | Mail | Mark one exact message read or unread | Implemented; Mail.app AppleScript scoped by account, mailbox, and RFC Message-ID | Implemented; current read/flag state fingerprint plus local Mail read-back |
| 16 | Reminders | Update title, notes, or priority for one exact reminder | Implemented; Swift EventKit helper with expected-state binding | Implemented; EventKit read-back plus notes-hash and priority checks |
| 17 | Calendar | Update title, time, location, notes, all-day state, exact alarm offsets, or explicit timed-event IANA time zone for one exact event | Implemented; Swift EventKit helper with expected-state binding | Implemented; EventKit read-back and unsupported-event refusal |
| 18 | Notes | Replace plaintext for one exact note | Implemented; Notes.app automation with exact handle and expected current hash | Implemented; exact-content read-back and new content hash |
| 19 | Calendar | Delete one exact timed or all-day event with expected alarm-offset and optional expected time-zone binding | Implemented; Swift EventKit helper with expected-state binding | Implemented; EventKit absence proof and unsupported-event refusal |
| 20 | Mail | Flag or unflag one exact message | Implemented; Mail.app AppleScript scoped by account, mailbox, and RFC Message-ID | Implemented; current read/flag state fingerprint plus local Mail read-back |
| 21 | Messages | Send one local file to one exact existing chat | Implemented; Messages.app AppleScript with exact chat handle and file-identity binding | Implemented; local `chat.db` outgoing attachment read-back and ghost-row detection |
| 22 | Notes | Create note in one exact selected folder | Implemented; Notes.app automation with exact `notes:folder:v1:` handle | Implemented; exact content read-back plus selected-folder proof |
| 23 | Mail | Archive one exact selected message | Implemented; Mail.app AppleScript with exact message handle and same-account Archive target | Implemented; current read/flag/mailbox fingerprint plus local Mail mailbox read-back |
| 24 | Mail | Move one exact selected message to Trash | Implemented; Mail.app AppleScript with exact message handle and same-account Trash target; no permanent delete | Implemented; current read/flag/mailbox fingerprint plus local Mail mailbox read-back |
| 25 | Notes | Delete one exact selected note | Implemented; Notes.app automation with exact note handle and expected current hash | Implemented; exact-handle absence proof |
| 26 | Mail | Send one bounded plaintext message | Implemented; Mail.app AppleScript with explicit recipient/subject/body approval and irreversible-send metadata | Implemented; local Sent-copy read-back when indexed, no sent body echo, partial result when read-back is delayed |
| 27 | Mail | Reply sender-only to one exact selected message | Implemented; Mail.app `reply` AppleScript scoped by account, mailbox, and RFC Message-ID, with exact message handle and source-state binding | Implemented; local Sent-copy read-back when indexed, no reply body echo, partial result when read-back is delayed |
| 27a | Mail | Reply-all to one exact selected message | Implemented; Mail.app `reply ... reply to all true` AppleScript scoped by account, mailbox, and RFC Message-ID, with exact message handle and source-state binding | Implemented; local Sent-copy read-back when indexed, no reply body echo, partial result when read-back is delayed |
| 28 | Notes | Move one exact selected note to one exact selected folder | Implemented; Notes.app automation with exact note/folder handles, expected current hash, and same-account target proof | Implemented; folder read-back proof and `body_returned:false` |
| 29 | Mail | Move one exact selected message to one exact target mailbox, including cross-account exact targets | Implemented; Mail.app AppleScript with exact message handle, exact `mail:mailbox:v1:` target, opaque source/target account refs for cross-account exact targets, and Trash/Junk-class target refusal | Implemented; current read/flag/mailbox fingerprint plus local Mail mailbox read-back |
| 30 | Contacts | Update name/organization/email/phone/URL fields on one exact contact | Implemented; Swift Contacts.framework helper with exact handle and `update_safe_sha256` binding; omitted method arrays preserve, provided arrays replace, empty arrays clear | Implemented; Contacts.framework read-back |
| 31 | Contacts | Delete one exact contact | Implemented; Swift Contacts.framework helper with exact handle and `delete_safe_sha256` binding | Implemented; Contacts.framework absence proof |
| 31a | Contacts | Append note text to one exact contact | Implemented; Swift Contacts.framework helper with exact handle and `note_safe_sha256` binding | Implemented; hash-only note read-back |
| 32 | Mail | Forward one exact selected no-attachment message with bounded plaintext prepend text | Implemented; Mail.app `forward` AppleScript scoped by account, mailbox, and RFC Message-ID | Implemented; local Sent-copy prefix read-back when indexed, no body/source-content echo, partial result when read-back is delayed |
| 33 | iCloud Drive | Replace text in one exact text file | Implemented; local filesystem with exact file handle and expected current hash | Implemented; iCloud Drive metadata, new content hash, and text content |
| 34 | iCloud Drive | Create one child folder under an exact parent folder | Implemented; local filesystem with exact parent handle and no-follow fd-relative mkdir | Implemented; iCloud Drive directory metadata without content hash |
| 35 | iCloud Drive | Move one exact text file to recoverable Trash | Implemented; local filesystem with exact file handle and expected current hash | Implemented; original-handle absence proof without returning raw Trash path |
| 36 | iCloud Drive | Rename/copy/move one exact text file | Implemented; local filesystem with exact file handle, optional exact parent handle, expected current hash, and no-overwrite fd-relative operations | Implemented; source/target presence proof and target content-hash read-back |
| 37 | Notes | Create one exact child folder under one exact normal parent folder | Implemented; Notes.app automation with exact `notes:folder:v1:` parent handle | Implemented; metadata-only read-back and selected-parent proof |
| 38 | Notes | Rename one exact normal folder | Implemented; Notes.app automation with exact `notes:folder:v1:` folder handle and expected folder-title hash | Implemented; metadata-only read-back and exact selected-folder proof |
| 39 | Notes | Delete one exact empty normal child folder | Implemented; Notes.app automation with exact `notes:folder:v1:` folder handle and expected folder-title hash | Implemented; exact selected-folder absence proof |
| 40 | iCloud Drive | Move one exact folder to one exact target parent folder | Implemented; local filesystem with exact directory handle, exact target-parent handle, expected directory metadata hash, and no-overwrite fd-relative move | Implemented; source/target metadata-only presence proof without content hash |
| 41 | iCloud Drive | Copy one exact empty folder to one exact target parent folder | Implemented; local filesystem with exact directory handle, exact target-parent handle, expected directory metadata hash, and no-overwrite fd-relative mkdir | Implemented; source-preservation and target metadata-only presence proof without content hash |
| 42 | Reminders | Move one exact reminder to one exact same-source target list | Implemented; Swift EventKit helper with exact reminder handle, exact expected current-list handle, exact same-source target-list handle, expected title, expected completed state, and expected current list name | Implemented; EventKit read-back `target_list_verified:true` identity proof |
| 43 | iCloud Drive | Permanently delete one exact empty folder | Implemented; local filesystem with exact directory handle, expected directory metadata hash, hidden staging identity proof, and permanent `rmdir` | Implemented; metadata-only original absence proof without content hash, content text, Trash path, or staging path |
| 44 | iCloud Drive | Permanently delete one exact supported text file | Implemented; local filesystem with exact file handle, expected current content hash, exact file identity approval binding, random-only hidden staging identity proof, and permanent unlink | Implemented; original absence proof without content hash, content text, raw path, Trash path, or staging path |
| 45 | Contacts | Create, rename, or delete one exact group; create contacts in one exact selected container | Implemented; Swift Contacts.framework helper with exact group/container handles and safe-hash binding | Implemented; metadata-only group read-back or absence proof with `contacts_deleted:false` |
| 46 | Mail | Add bounded caller-selected local file attachments to create-draft/send/reply/reply-all/forward | Implemented; Mail.app outgoing-message attachment creation with local file identity plus content SHA-256 approval binding, private-copy automation handoff, and no path/bytes output | Implemented; Drafts/Sent read-back when indexed plus Mail automation attachment-count confirmation |
| 47 | Mail | Date-bounded body/header/attachment discovery for audits | Implemented read-only; local `.emlx` body/header/attachment filename parsing with date bounds, query guards, capped snippets/results, cursor pagination, and no durable body index in this gate | Implemented; opaque message/attachment handles, masked headers, snippets only, and exact-handle content/export gates |
| 48 | Mail | Attachment text/PDF/OCR discovery for audits | Implemented read-only; exact message/attachment metadata discovery can opt into bounded local text/PDF/OCR snippets with no bytes, paths, or durable index in this gate | Implemented; capped redacted snippets only, exact attachment export still required for bytes |
| 49 | Mail | Capped exact bulk triage | Implemented; unique exact `mail:message:v2:` handles for read/flag/archive/move/trash operations with max 20 selected handles and whole-batch preflight | Implemented; per-message read/flag/mailbox-state binding, partial failure reporting, and no query-result auto-apply |
| 50 | Mail | Opt-in source attachment-like part forwarding | Implemented; exact `forward_message` may set `include_source_attachments` to let Mail.app preserve source attachment-like parts after uncapped source-part and source-content state binding | Implemented; pre-send Mail attachment-count verification, no source attachment bytes/paths/body or per-part Sent proof, default refusal unchanged |
| 51 | Mail | Opt-in private FTS index for audit discovery | Implemented local cache write/read; date-bounded build requires confirmation, can include attachment text/PDF/OCR, and never returns the cache path | Implemented; live-row revalidation, exact message handles, matched scopes, capped redacted snippets, and no body/bytes/cache-path output |
| 52 | Mail | Signature metadata, plugin templates, and capped query-result triage planning | Implemented; signatures are opaque metadata without bodies, templates are plugin-local explicit user content, and FTS/search results can become capped exact bulk triage plans | Implemented; no signature body return, no raw template path, no query-result apply without exact selected handles and bulk-triage token |
| 53 | Mail | Synthetic mailbox management and synthetic-only cleanup | Implemented; create/rename only top-level empty `LAD-TEST-*` mailboxes, permanent-delete/empty Trash/Junk only synthetic `LAD-TEST-*` messages | Implemented; Mail.app public automation plus local read-back create/rename or absence proof, no raw account IDs, no real non-synthetic cleanup |

No first-tranche tool should overwrite outside an approved exact-handle replace-text gate, bulk edit outside approved exact batch gates, or manage real account state. Delete requires a separate destructive design gate and absence proof; the approved examples are Reminders exact-handle delete, Calendar exact-event delete, Contacts exact-handle delete, Notes exact-note delete, Notes exact empty child-folder delete, iCloud Drive exact selected-folder permanent delete, iCloud Drive exact text-file permanent delete, and synthetic-only Mail cleanup under `docs/V1_84_MAIL_SYNTHETIC_MAILBOX_CLEANUP_WRITE_DESIGN.md`. Mail move-to-Trash is reversible removal only and is governed by `docs/V1_41_MAIL_TRASH_WRITE_DESIGN.md`. The approved send operations are bounded exact-chat Messages send-text/send-file gates documented in `docs/V1_24_MESSAGES_SEND_TEXT_WRITE_DESIGN.md` and `docs/V1_38_MESSAGES_SEND_FILE_WRITE_DESIGN.md`, bounded irreversible Mail send-message documented in `docs/V1_43_MAIL_SEND_WRITE_DESIGN.md`, exact-message sender-only Mail reply-message documented in `docs/V1_44_MAIL_REPLY_WRITE_DESIGN.md`, exact-message Mail reply-all-message documented in `docs/V1_73_MAIL_REPLY_ALL_WRITE_DESIGN.md`, exact-message Mail forward-message documented in `docs/V1_50_MAIL_FORWARD_WRITE_DESIGN.md`, and opt-in source attachment-like part forwarding documented in `docs/V1_80_MAIL_SOURCE_FORWARD_WRITE_DESIGN.md`; richer Messages mutation remains deferred.

## Deferred Write Tranches

These need separate design documents:

- Contact free-form custom labels are implemented and no longer deferred: labels are preserved verbatim (exact case, spaces, punctuation) up to 255 characters, rejecting only control characters and oversize labels, through the existing exact create/update gate, governed by `docs/V1_180A_CONTACTS_FREEFORM_LABELS_DESIGN.md`. Still deferred for Contacts: raw identifier input, broad contact dumps in chat, direct Contacts database writes, duplicate merge automation, and group CRUD outside the exact group/container gate.
- Calendar delete outside the approved exact-event absence-proof gate, attendees, silent default-calendar guessing or default-calendar mutation, timed-event time-zone inference, availability outside explicit support-mask validation, non-allow-listed event URL schemes, procedure alarms, and update outside the approved exact-event title/time/location/structured-location/notes/all-day/relative-or-absolute display/audio/email/geofence alarm/time-zone/availability/event-URL/simple-add-recurrence gate.
- Reminder list/account management outside exact same-source list-move and exact list create/rename/empty-delete/same-source migrate-delete, cross-source migrate-delete, attachments, URL mutation outside the approved exact URL update/clear gate, alarm mutation outside exact absolute/relative/mixed display-alarm set/clear/start-date set/clear/recurrence create/update/clear, mixed-with-audio/email/geofence/procedure Reminder alarms, raw alarm-state return, rich-content mutation, delete outside the approved exact-handle gate, list moves outside the approved exact expected-current-list plus target-list identity-proof gate, and title/notes/priority mutation outside the approved exact-handle gates.
- Notes rich-text body read and rich-text body create/replace are implemented and no longer deferred: `content_format:html` returns the bounded rich-text body plus extracted visible text behind the exact `notes:note:v2:` handle gate, and `create_html`/`replace_html` apply sanitized HTML with a semantic (extracted visible-text) read-back, governed by `docs/V1_179_NOTES_RICH_TEXT_BODY_WRITE_DESIGN.md`. Still deferred for Notes: update outside the approved exact-note plaintext append/replace and rich-text create/replace gates, delete outside the approved exact-note delete gate, move outside the approved exact-note/exact-folder move-to-folder gate, folder/account targeting outside exact note create, exact child-folder create, exact-folder rename, exact empty child-folder delete, exact empty child-folder move, and move by `notes:folder:v1:` handle, root/non-empty/recursive folder delete, root/non-empty/recursive/cross-account folder move, root/default-account folder creation, attachment mutation, broad attachment export, broad body search, checklist state, locked/shared-note mutation, Recently Deleted management, and bulk operations.
- iCloud Drive folder creation outside the approved exact create-folder or bounded create-folder-path gates, folder rename outside the approved exact folder rename gate, folder Trash outside the approved exact folder trash gate, folder delete outside the approved exact selected-folder delete gate, folder move outside the approved exact folder move gate, folder copy outside the approved exact selected-folder copy gate, unbounded recursive folder creation, unbounded recursive folder copy/delete, content replacement outside the approved exact replace-text or replace-file gates, trash/delete outside the approved exact trash-text, exact folder trash, exact regular-file trash, or exact delete-file gates, rename/copy/move outside the approved exact text-file or regular-file gates, file permanent delete outside the approved exact delete-text or delete-file gates, empty Trash, binary/document content generation, and regular-file mutation outside exact import-file, exact replace-file, exact trash-file, exact delete-file, or metadata-only rename/copy/move gates.
- Home-directory Filesystem CRUD is implemented and no longer deferred: `local-apple-data filesystem` / `filesystem_*` reuse the exact iCloud Drive plan/apply/read-back gates re-rooted at the operator home directory (`~`) through the `fs:file:v1:` handle namespace, governed by `docs/V1_178_FILESYSTEM_HOME_SCOPE_WRITE_DESIGN.md`. Still deferred for this surface: any target outside the home root (`..`/symlink escape, other users' home directories, `/System`, `/Library` system, `/usr`, `/bin`, `/etc`, `/private`), content-read or mutation of credential/secret paths (`~/.ssh`, `~/.aws`, `~/.gnupg`, `~/.config/gh`, `~/.config/gcloud`, `~/.netrc`, `~/.docker/config.json`, `~/.kube`, `~/Library/Keychains`, `~/Library/Application Support/com.apple.TCC`, and any `.env`/`.env.*`) unless `LOCAL_APPLE_DATA_FS_ALLOW_CREDENTIAL_PATHS=1`, empty Trash, binary/document content generation, hidden-file write, and everything already deferred for iCloud Drive above.
- Mail send outside the approved send-message gate, sender selection outside the approved draft/send/reply/reply-all/forward sender gate, reply outside the approved exact-message sender-only or reply-all gates, forward outside the approved exact-message forward-message gate, attachment mutation outside approved draft/send/reply/reply-all/forward local-file attachment gates, Mail source attachment/non-body-part forwarding outside explicit `include_source_attachments`, cross-account move outside the approved exact target-mailbox gate, Trash/Junk-class target through move-message, permanent delete outside synthetic `LAD-TEST-*` cleanup, empty Trash/Junk outside synthetic-only cleanup, flag mutation outside approved exact-message flag/unflag, read/flag/archive/move/trash triage outside approved exact-message or capped exact bulk mark-read/mark-unread/flag-message/unflag-message/archive-message/move-message/trash-message, query-result triage outside capped preview-to-exact-plan flow, mailbox management outside empty top-level `LAD-TEST-*` mailboxes, account management, unbounded/bulk Mail body search, background Mail indexing, broad attachment export, arbitrary attachment document extraction beyond v1.79/v1.81 text/PDF/OCR snippets, HTML/rich-text draft mutation, template mutation outside plugin-managed local templates, or unbounded bulk mutation.
- Messages direct-recipient sends, new chat creation, SMS fallback selection, outgoing-account selection, rich text, effects, inline replies, reactions/tapbacks, edit, unsend, delete, mark read, group management, participant lookup for mutation, contact lookup, file-send outside the approved exact-chat send-file gate, and bulk operations. Read-only participant metadata is limited by `docs/V1_64_MESSAGES_PARTICIPANTS_METADATA.md`; the current source-review blocker for risky mutation is `docs/V1_47_MESSAGES_RISKY_MUTATION_SOURCE_REVIEW.md`, backed by `uv run python scripts/audit_messages_public_surface.py --json`.
- Photos asset content edits, delete outside the exact selected-asset absence-proof gate, permanent delete/Recently Deleted empty, smart/shared/synced album targeting, bulk membership outside exact regular-album add/remove, metadata mutation outside exact favorite/hidden update, thumbnails, inline asset bytes, network iCloud fetch, and bulk operations.
- Voice Memos creation, generated transcription, or deletion.
- Safari bookmark create/update/delete/move remains deferred and is REFUSED as infeasible-to-do-safely on this platform. Evidence: the authoritative Safari scripting dictionary (`/Applications/Xcode.app/Contents/Developer/usr/bin/sdef /Applications/Safari.app`) exposes exactly one scripting class (`tab`) and no bookmark class and no bookmark create/edit/delete commands; the only write-adjacent commands are `add reading list item` (append-only Reading List, no edit/delete/read), `do JavaScript`, and `show bookmarks`. The only remaining path is editing the binary `~/Library/Safari/Bookmarks.plist` directly, which is unsafe: it is a CloudKit-synced store (every bookmark carries a `Sync` dict with a `ServerID` and an opaque nested-bplist `Data` blob), Safari holds an in-memory copy that clobbers on-disk writes, and writing entries without valid undocumented `Sync` payloads risks iCloud sync divergence/corruption across the account. No read-back-provable, non-corrupting write path exists, so Safari bookmark mutation stays blocked. Also deferred: Safari history/open-tabs/iCloud-tabs access, browser-state access, cookies, passwords, page content, broad recursive dumps, and folder mutation.
- Shortcuts run of one exact identifier-bound shortcut is implemented and no longer deferred: `local-apple-data shortcuts plan`/`apply` and `shortcuts_plan_run`/`shortcuts_apply_run` gate `operation:run` behind an exact `shortcuts:item:v1:` handle resolved to a stable identifier bound into the approval fingerprint, argv-only invocation, a hard execution timeout, and an invocation-only proof (a shortcut's arbitrary side effects are not verifiable by read-back), governed by `docs/V1_180C_SHORTCUTS_RUN_WRITE_DESIGN.md`. Still deferred for Shortcuts: open/view/sign/export, shortcut body/action graph reads, run by fuzzy or raw name, inline shortcut definitions, Shortcuts SQLite scraping, import, create, update, delete, duplicate, or other mutation.
- Books book/chapter/PDF/EPUB text extraction, broad library or annotation dumps/searches, iCloud fetch, Books.app automation, or mutation.
- Podcasts transcript/audio/feed/enclosure URL extraction, broad library or episode-description dumps/searches, iCloud media fetch, Podcasts.app automation, or mutation.
- Music audio/lyrics/file-path export, play history, ratings/favorites, broad playlist track dumps, playback/queue control, library import/delete, raw Music database parsing, iCloud media fetch, Music.app automation beyond bounded read-only metadata, or mutation.
- TV video/file-path/artwork/description export, playback or watched state, ratings/favorites, broad playlist item dumps, playback/queue control, library import/delete, raw TV library parsing, iCloud media fetch, TV.app automation beyond bounded read-only metadata, or mutation.
- Freeform board BLOB/CRDT decoding, board title/content export, decoded board item dumps, asset export, previews, collaboration payloads, broad dumps/searches, raw Freeform database parsing, iCloud web/API access, Freeform.app automation, or mutation.
- Authoritative Hide My Email inventory or mutation.

## Required Tests

Each mutation tranche must add:

- Synthetic preview tests.
- Synthetic apply/read-back tests with mocked Apple helpers.
- Invalid-handle and stale-handle tests.
- Permission-denied tests.
- Partial-failure tests.
- Runtime verifier coverage for tool annotations and refusal behavior.
- Mutation-gate auditor coverage for approved tool names.
- Write-design gate auditor coverage for the operation's design-only or approved-with-tests state.
- Redaction scan coverage for logs and docs.

## Required Runtime Safeguards

- Default to preview.
- Require exact target handles for edits to existing objects.
- Require explicit target container selection for creates.
- Return structured warning/error codes.
- Log only command name, status, warning codes, counts, and duration.
- Do not log proposed content, handles, raw paths, account identifiers, framework identifiers, recipients, attendees, or aliases.
- Refuse broad batch mutation; Contacts allows only the approved capped exact batch gate.

## Still Blocked

The following remain blocked until a separate source review proves a durable, local, privacy-respecting path:

- Hide My Email alias creation, deactivation, deletion, or authoritative inventory.
- Any iCloud.com, browser-session, cookie, keychain, private iCloud web/API, IMAP, OAuth, or connector fallback mutation path.
- Messages direct-recipient sends, new chats, SMS fallback selection, file-send outside the approved exact-chat send-file gate, reactions, edit, delete, mark read, account selection, or login/logout automation; see `docs/V1_47_MESSAGES_RISKY_MUTATION_SOURCE_REVIEW.md` and the machine-readable public SDEF audit.
- Mail sending outside the approved send-message gate or Mail management from the plugin before a separate source review and explicit design gate.
- Photos permanent delete/Recently Deleted empty, smart/shared/synced album targeting, bulk membership outside exact regular-album add/remove, content edits, or metadata mutation outside exact favorite/hidden update and exact selected-asset delete before a separate PhotoKit write design.
- Voice Memos generated transcription or mutation before a separate media-content design.
